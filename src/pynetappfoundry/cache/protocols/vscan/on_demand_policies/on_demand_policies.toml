[endpoint]
path = "/protocols/vscan/{svm.uuid}/on-demand-policies"
schema = "vscan_on_demand"
class_name = "OntapVscanOnDemand"

[fields.log_path]
cache_strategy = "cache"

[fields.name]
cache_strategy = "cache"

[fields.scan_paths]
cache_strategy = "cache"

[fields.schedule_name]
cache_strategy = "cache"

[fields.schedule_uuid]
cache_strategy = "cache"

[fields.scope_exclude_extensions]
cache_strategy = "cache"

[fields.scope_exclude_paths]
cache_strategy = "cache"

[fields.scope_include_extensions]
cache_strategy = "cache"

[fields.scope_max_file_size]
cache_strategy = "cache"

[fields.scope_scan_without_extension]
cache_strategy = "cache"

[fields.svm_name]
cache_strategy = "cache"

[fields.svm_uuid]
cache_strategy = "cache"
