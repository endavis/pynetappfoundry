[endpoint]
path = "/support/ems/destinations"
schema = "ems_destination_response"
class_name = "OntapEmsDestinationResponse"

[fields."access_control_role.name"]
cache_strategy = "cache"

[fields."certificate.ca"]
cache_strategy = "cache"

[fields."certificate.name"]
cache_strategy = "cache"

[fields."certificate.serial_number"]
cache_strategy = "cache"

[fields."connectivity.errors"]
cache_strategy = "cache"

[fields."connectivity.state"]
cache_strategy = "cache"

[fields.destination]
cache_strategy = "cache"

[fields.filters]
cache_strategy = "cache"

[fields.name]
cache_strategy = "cache"

[fields."syslog.format.hostname_override"]
cache_strategy = "cache"

[fields."syslog.format.message"]
cache_strategy = "cache"

[fields."syslog.format.timestamp_override"]
cache_strategy = "cache"

[fields."syslog.port"]
cache_strategy = "cache"

[fields."syslog.transport"]
cache_strategy = "cache"

[fields.system_defined]
cache_strategy = "cache"

[fields.type_]
cache_strategy = "cache"
