[endpoint]
path = "/support/auto-update"
schema = "auto_update_info"
class_name = "OntapAutoUpdateInfo"

[fields.enabled]
cache_strategy = "cache"

[fields."eula.accepted"]
cache_strategy = "cache"

[fields."eula.accepted_ip_address"]
cache_strategy = "cache"

[fields."eula.accepted_timestamp"]
cache_strategy = "cache"

[fields."eula.user_id_accepted"]
cache_strategy = "cache"
