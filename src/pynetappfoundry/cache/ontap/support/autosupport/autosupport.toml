[endpoint]
path = "/support/autosupport"
schema = "autosupport"
class_name = "OntapAutosupport"

[fields.component]
cache_strategy = "cache"

[fields."corrective_action.code"]
cache_strategy = "cache"

[fields."corrective_action.message"]
cache_strategy = "cache"

[fields.destination]
cache_strategy = "cache"

[fields."issue.code"]
cache_strategy = "cache"

[fields."issue.message"]
cache_strategy = "cache"

[fields."node.name"]
cache_strategy = "cache"

[fields."node.uuid"]
cache_strategy = "cache"
