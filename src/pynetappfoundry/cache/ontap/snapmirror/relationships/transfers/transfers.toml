[endpoint]
path = "/snapmirror/relationships/{relationship.uuid}/transfers"
schema = "snapmirror_transfer"
class_name = "OntapSnapmirrorTransfer"

[fields.bytes_transferred]
cache_strategy = "realtime"

[fields.checkpoint_size]
cache_strategy = "realtime"

[fields.end_time]
cache_strategy = "realtime"

[fields."error_info.code"]
cache_strategy = "cache"

[fields."error_info.message"]
cache_strategy = "cache"

[fields.files]
cache_strategy = "realtime"

[fields.last_updated_time]
cache_strategy = "realtime"

[fields.network_compression_ratio]
cache_strategy = "realtime"

[fields.on_demand_attrs]
cache_strategy = "cache"

[fields.options]
cache_strategy = "cache"

[fields."relationship.destination.cluster.name"]
cache_strategy = "cache"

[fields."relationship.destination.cluster.uuid"]
cache_strategy = "cache"

[fields."relationship.destination.consistency_group_volumes"]
cache_strategy = "cache"

[fields."relationship.destination.ipspace"]
cache_strategy = "cache"

[fields."relationship.destination.luns.name"]
cache_strategy = "cache"

[fields."relationship.destination.luns.uuid"]
cache_strategy = "cache"

[fields."relationship.destination.path"]
cache_strategy = "cache"

[fields."relationship.destination.svm.name"]
cache_strategy = "cache"

[fields."relationship.destination.svm.uuid"]
cache_strategy = "cache"

[fields."relationship.restore"]
cache_strategy = "cache"

[fields."relationship.uuid"]
cache_strategy = "cache"

[fields.snapshot]
cache_strategy = "cache"

[fields.source_snapshot]
cache_strategy = "cache"

[fields.state]
cache_strategy = "realtime"

[fields.storage_efficiency_enabled]
cache_strategy = "cache"

[fields.throttle]
cache_strategy = "cache"

[fields.total_duration]
cache_strategy = "realtime"

[fields.uuid]
cache_strategy = "cache"
