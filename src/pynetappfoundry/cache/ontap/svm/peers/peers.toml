[endpoint]
path = "/svm/peers"
schema = "svm_peer"
class_name = "OntapSvmPeer"

[fields.applications]
cache_strategy = "cache"

[fields.force]
cache_strategy = "cache"

[fields.name]
cache_strategy = "cache"

[fields."peer.cluster.name"]
cache_strategy = "cache"

[fields."peer.cluster.uuid"]
cache_strategy = "cache"

[fields."peer.svm.name"]
cache_strategy = "cache"

[fields."peer.svm.uuid"]
cache_strategy = "cache"

[fields.state]
cache_strategy = "cache"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"

[fields.uuid]
cache_strategy = "cache"
