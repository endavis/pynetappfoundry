[endpoint]
path = "/svm/svms"
schema = "svm"
class_name = "OntapSvm"

[fields.aggregates]
cache_strategy = "cache"

[fields.aggregates_delegated]
cache_strategy = "cache"

[fields."anti_ransomware.event_log.is_enabled_on_new_file_extension_seen"]
cache_strategy = "cache"

[fields."anti_ransomware.event_log.is_enabled_on_snapshot_copy_creation"]
cache_strategy = "cache"

[fields.anti_ransomware_auto_switch_duration_without_new_file_extension]
cache_strategy = "cache"

[fields.anti_ransomware_auto_switch_from_learning_to_enabled]
cache_strategy = "cache"

[fields.anti_ransomware_auto_switch_minimum_file_count]
cache_strategy = "cache"

[fields.anti_ransomware_auto_switch_minimum_file_extension]
cache_strategy = "cache"

[fields.anti_ransomware_auto_switch_minimum_incoming_data]
cache_strategy = "cache"

[fields.anti_ransomware_auto_switch_minimum_learning_period]
cache_strategy = "cache"

[fields.anti_ransomware_default_volume_state]
cache_strategy = "cache"

[fields.anti_ransomware_incoming_write_threshold]
cache_strategy = "cache"

[fields.anti_ransomware_incoming_write_threshold_percent]
cache_strategy = "cache"

[fields.auto_enable_activity_tracking]
cache_strategy = "cache"

[fields.auto_enable_analytics]
cache_strategy = "cache"

[fields."certificate.name"]
cache_strategy = "cache"

[fields."certificate.uuid"]
cache_strategy = "cache"

[fields."cifs.ad_domain.default_site"]
cache_strategy = "cache"

[fields."cifs.ad_domain.fqdn"]
cache_strategy = "cache"

[fields."cifs.ad_domain.organizational_unit"]
cache_strategy = "cache"

[fields."cifs.ad_domain.password"]
cache_strategy = "cache"

[fields."cifs.ad_domain.user"]
cache_strategy = "cache"

[fields."cifs.allowed"]
cache_strategy = "cache"

[fields."cifs.auth_style"]
cache_strategy = "cache"

[fields."cifs.domain_workgroup"]
cache_strategy = "cache"

[fields."cifs.enabled"]
cache_strategy = "cache"

[fields."cifs.name"]
cache_strategy = "cache"

[fields."cifs.workgroup"]
cache_strategy = "cache"

[fields.comment]
cache_strategy = "cache"

[fields."dns.domains"]
cache_strategy = "cache"

[fields."dns.servers"]
cache_strategy = "cache"

[fields.fc_interfaces]
cache_strategy = "cache"

[fields."fcp.allowed"]
cache_strategy = "cache"

[fields."fcp.enabled"]
cache_strategy = "cache"

[fields.ip_interfaces]
cache_strategy = "cache"

[fields."ipspace.name"]
cache_strategy = "cache"

[fields."ipspace.uuid"]
cache_strategy = "cache"

[fields.is_space_enforcement_logical]
cache_strategy = "cache"

[fields.is_space_reporting_logical]
cache_strategy = "cache"

[fields."iscsi.allowed"]
cache_strategy = "cache"

[fields."iscsi.enabled"]
cache_strategy = "cache"

[fields.language]
cache_strategy = "cache"

[fields."ldap.ad_domain"]
cache_strategy = "cache"

[fields."ldap.base_dn"]
cache_strategy = "cache"

[fields."ldap.bind_dn"]
cache_strategy = "cache"

[fields."ldap.enabled"]
cache_strategy = "cache"

[fields."ldap.restrict_discovery_to_site"]
cache_strategy = "cache"

[fields."ldap.servers"]
cache_strategy = "cache"

[fields.max_volumes]
cache_strategy = "cache"

[fields.name]
cache_strategy = "cache"

[fields."ndmp.allowed"]
cache_strategy = "cache"

[fields."nfs.allowed"]
cache_strategy = "cache"

[fields."nfs.enabled"]
cache_strategy = "cache"

[fields."nis.domain"]
cache_strategy = "cache"

[fields."nis.enabled"]
cache_strategy = "cache"

[fields."nis.servers"]
cache_strategy = "cache"

[fields."nsswitch.group"]
cache_strategy = "cache"

[fields."nsswitch.hosts"]
cache_strategy = "cache"

[fields."nsswitch.namemap"]
cache_strategy = "cache"

[fields."nsswitch.netgroup"]
cache_strategy = "cache"

[fields."nsswitch.passwd"]
cache_strategy = "cache"

[fields.number_of_volumes_in_recovery_queue]
cache_strategy = "cache"

[fields."nvme.allowed"]
cache_strategy = "cache"

[fields."nvme.enabled"]
cache_strategy = "cache"

[fields."qos_adaptive_policy_group_template.max_throughput_iops"]
cache_strategy = "cache"

[fields."qos_adaptive_policy_group_template.max_throughput_mbps"]
cache_strategy = "cache"

[fields."qos_adaptive_policy_group_template.min_throughput_iops"]
cache_strategy = "cache"

[fields."qos_adaptive_policy_group_template.min_throughput_mbps"]
cache_strategy = "cache"

[fields."qos_adaptive_policy_group_template.name"]
cache_strategy = "cache"

[fields."qos_adaptive_policy_group_template.uuid"]
cache_strategy = "cache"

[fields."qos_policy.max_throughput_iops"]
cache_strategy = "cache"

[fields."qos_policy.max_throughput_mbps"]
cache_strategy = "cache"

[fields."qos_policy.min_throughput_iops"]
cache_strategy = "cache"

[fields."qos_policy.min_throughput_mbps"]
cache_strategy = "cache"

[fields."qos_policy.name"]
cache_strategy = "cache"

[fields."qos_policy.uuid"]
cache_strategy = "cache"

[fields."qos_policy_group_template.max_throughput_iops"]
cache_strategy = "cache"

[fields."qos_policy_group_template.max_throughput_mbps"]
cache_strategy = "cache"

[fields."qos_policy_group_template.min_throughput_iops"]
cache_strategy = "cache"

[fields."qos_policy_group_template.min_throughput_mbps"]
cache_strategy = "cache"

[fields."qos_policy_group_template.name"]
cache_strategy = "cache"

[fields."qos_policy_group_template.uuid"]
cache_strategy = "cache"

[fields.routes]
cache_strategy = "cache"

[fields."s3.allowed"]
cache_strategy = "cache"

[fields."s3.certificate.name"]
cache_strategy = "cache"

[fields."s3.certificate.uuid"]
cache_strategy = "cache"

[fields."s3.default_unix_user"]
cache_strategy = "cache"

[fields."s3.default_win_user"]
cache_strategy = "cache"

[fields."s3.enabled"]
cache_strategy = "cache"

[fields."s3.is_http_enabled"]
cache_strategy = "cache"

[fields."s3.is_https_enabled"]
cache_strategy = "cache"

[fields."s3.name"]
cache_strategy = "cache"

[fields."s3.port"]
cache_strategy = "cache"

[fields."s3.secure_port"]
cache_strategy = "cache"

[fields."snapmirror.is_protected"]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields."snapmirror.protected_consistency_group_count"]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields."snapmirror.protected_volumes_count"]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields."snapshot_policy.name"]
cache_strategy = "cache"

[fields."snapshot_policy.uuid"]
cache_strategy = "cache"

[fields.state]
cache_strategy = "cache"

[fields."storage.allocated"]
cache_strategy = "cache"

[fields."storage.available"]
cache_strategy = "cache"

[fields."storage.limit"]
cache_strategy = "cache"

[fields."storage.limit_threshold_alert"]
cache_strategy = "cache"

[fields."storage.limit_threshold_exceeded"]
cache_strategy = "cache"

[fields."storage.used_percentage"]
cache_strategy = "cache"

[fields.subtype]
cache_strategy = "cache"

[fields.total_volume_size_in_recovery_queue]
cache_strategy = "cache"

[fields.uuid]
cache_strategy = "cache"
