[endpoint]
path = "/svm/svms/{svm.uuid}/top-metrics/clients"
schema = "top_metrics_svm_client"
class_name = "OntapTopMetricsSvmClient"

[fields.client_ip]
cache_strategy = "cache"

[fields."iops.error.lower_bound"]
cache_strategy = "cache"

[fields."iops.error.upper_bound"]
cache_strategy = "cache"

[fields."iops.read"]
cache_strategy = "cache"

[fields."iops.write"]
cache_strategy = "cache"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"

[fields."throughput.error.lower_bound"]
cache_strategy = "cache"

[fields."throughput.error.upper_bound"]
cache_strategy = "cache"

[fields."throughput.read"]
cache_strategy = "cache"

[fields."throughput.write"]
cache_strategy = "cache"
