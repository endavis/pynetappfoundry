[endpoint]
path = "/svm/svms/{svm.uuid}/top-metrics/users"
schema = "top_metrics_svm_user"
class_name = "OntapTopMetricsSvmUser"

[fields."iops.error.lower_bound"]
cache_strategy = "cache"

[fields."iops.error.upper_bound"]
cache_strategy = "cache"

[fields."iops.read"]
cache_strategy = "cache"

[fields."iops.write"]
cache_strategy = "cache"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"

[fields."throughput.error.lower_bound"]
cache_strategy = "cache"

[fields."throughput.error.upper_bound"]
cache_strategy = "cache"

[fields."throughput.read"]
cache_strategy = "cache"

[fields."throughput.write"]
cache_strategy = "cache"

[fields.user_id]
cache_strategy = "cache"

[fields.user_name]
cache_strategy = "cache"

[fields.volumes]
cache_strategy = "cache"
