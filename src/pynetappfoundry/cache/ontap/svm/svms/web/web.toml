[endpoint]
path = "/svm/svms/{svm.uuid}/web"
schema = "web_svm"
class_name = "OntapWebSvm"

[fields."certificate.name"]
cache_strategy = "cache"

[fields."certificate.uuid"]
cache_strategy = "cache"

[fields.client_enabled]
cache_strategy = "cache"

[fields.ocsp_enabled]
cache_strategy = "cache"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"
