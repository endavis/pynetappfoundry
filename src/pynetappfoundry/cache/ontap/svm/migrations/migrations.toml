[endpoint]
path = "/svm/migrations"
schema = "svm_migration"
class_name = "OntapSvmMigration"

[fields.auto_cutover]
cache_strategy = "cache"

[fields.auto_source_cleanup]
cache_strategy = "cache"

[fields.check_only]
cache_strategy = "cache"

[fields.current_operation]
cache_strategy = "cache"

[fields."destination.ipspace.name"]
cache_strategy = "cache"

[fields."destination.ipspace.uuid"]
cache_strategy = "cache"

[fields."destination.volume_placement.aggregates"]
cache_strategy = "cache"

[fields."destination.volume_placement.volume_aggregate_pairs"]
cache_strategy = "cache"

[fields."ip_interface_placement.ip_interfaces"]
cache_strategy = "cache"

[fields.last_failed_state]
cache_strategy = "cache"

[fields.last_operation]
cache_strategy = "cache"

[fields.messages]
cache_strategy = "cache"

[fields.point_of_no_return]
cache_strategy = "cache"

[fields.restart_count]
cache_strategy = "cache"

[fields."source.cluster.name"]
cache_strategy = "cache"

[fields."source.cluster.uuid"]
cache_strategy = "cache"

[fields."source.svm.name"]
cache_strategy = "cache"

[fields."source.svm.uuid"]
cache_strategy = "cache"

[fields.throttle]
cache_strategy = "cache"

[fields."time_metrics.cutover_complete_time"]
cache_strategy = "cache"

[fields."time_metrics.cutover_start_time"]
cache_strategy = "cache"

[fields."time_metrics.cutover_trigger_time"]
cache_strategy = "cache"

[fields."time_metrics.end_time"]
cache_strategy = "cache"

[fields."time_metrics.last_pause_time"]
cache_strategy = "cache"

[fields."time_metrics.last_resume_time"]
cache_strategy = "cache"

[fields."time_metrics.start_time"]
cache_strategy = "cache"

[fields.uuid]
cache_strategy = "cache"
