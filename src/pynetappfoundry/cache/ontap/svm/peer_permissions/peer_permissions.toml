[endpoint]
path = "/svm/peer-permissions"
schema = "svm_peer_permission"
class_name = "OntapSvmPeerPermission"

[fields.applications]
cache_strategy = "cache"

[fields."cluster_peer.name"]
cache_strategy = "cache"

[fields."cluster_peer.uuid"]
cache_strategy = "cache"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"
