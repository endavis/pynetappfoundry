[endpoint]
path = "/security/key-manager-configs"
schema = "key_manager_config"
class_name = "OntapKeyManagerConfig"

[fields.cc_mode_enabled]
cache_strategy = "cache"

[fields.cloud_kms_retry_count]
cache_strategy = "cache"

[fields."health_monitor_policy.akv.enabled"]
cache_strategy = "cache"

[fields."health_monitor_policy.akv.manage_volume_offline"]
cache_strategy = "cache"

[fields."health_monitor_policy.aws.enabled"]
cache_strategy = "cache"

[fields."health_monitor_policy.aws.manage_volume_offline"]
cache_strategy = "cache"

[fields."health_monitor_policy.gcp.enabled"]
cache_strategy = "cache"

[fields."health_monitor_policy.gcp.manage_volume_offline"]
cache_strategy = "cache"

[fields."health_monitor_policy.ikp.enabled"]
cache_strategy = "cache"

[fields."health_monitor_policy.ikp.manage_volume_offline"]
cache_strategy = "cache"

[fields."health_monitor_policy.kmip.enabled"]
cache_strategy = "cache"

[fields."health_monitor_policy.kmip.manage_volume_offline"]
cache_strategy = "cache"

[fields."health_monitor_policy.okm.enabled"]
cache_strategy = "cache"

[fields."health_monitor_policy.okm.manage_volume_offline"]
cache_strategy = "cache"

[fields.health_monitor_polling_interval]
cache_strategy = "cache"

[fields.passphrase]
cache_strategy = "cache"
