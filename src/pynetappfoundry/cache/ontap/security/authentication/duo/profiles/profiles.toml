[endpoint]
path = "/security/authentication/duo/profiles"
schema = "duo"
class_name = "OntapDuo"

[fields.api_host]
cache_strategy = "cache"

[fields.auto_push]
cache_strategy = "cache"

[fields.comment]
cache_strategy = "cache"

[fields.fail_mode]
cache_strategy = "cache"

[fields.fingerprint]
cache_strategy = "cache"

[fields.http_proxy]
cache_strategy = "cache"

[fields.integration_key]
cache_strategy = "cache"

[fields.is_enabled]
cache_strategy = "cache"

[fields.max_prompts]
cache_strategy = "cache"

[fields.owner_name]
cache_strategy = "cache"

[fields.owner_uuid]
cache_strategy = "cache"

[fields.push_info]
cache_strategy = "cache"

[fields.secret_key]
cache_strategy = "cache"

[fields.status]
cache_strategy = "cache"
