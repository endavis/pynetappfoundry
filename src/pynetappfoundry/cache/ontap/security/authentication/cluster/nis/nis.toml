[endpoint]
path = "/security/authentication/cluster/nis"
schema = "cluster_nis_service"
class_name = "OntapClusterNisService"

[fields.server]
cache_strategy = "cache"

[fields."status.code"]
cache_strategy = "cache"

[fields."status.message"]
cache_strategy = "cache"
