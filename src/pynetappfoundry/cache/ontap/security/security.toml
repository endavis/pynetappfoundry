[endpoint]
path = "/security"
schema = "security_config"
class_name = "OntapSecurityConfig"

[fields."fips.enabled"]
cache_strategy = "cache"

[fields."management_protocols.rsh_enabled"]
cache_strategy = "cache"

[fields."management_protocols.telnet_enabled"]
cache_strategy = "cache"

[fields."onboard_key_manager_configurable_status.code"]
cache_strategy = "cache"

[fields."onboard_key_manager_configurable_status.message"]
cache_strategy = "cache"

[fields."onboard_key_manager_configurable_status.supported"]
cache_strategy = "cache"

[fields."software_data_encryption.conversion_enabled"]
cache_strategy = "cache"

[fields."software_data_encryption.disabled_by_default"]
cache_strategy = "cache"

[fields."software_data_encryption.encryption_state"]
cache_strategy = "cache"

[fields."software_data_encryption.rekey"]
cache_strategy = "cache"

[fields."tls.cipher_suites"]
cache_strategy = "cache"

[fields."tls.protocol_versions"]
cache_strategy = "cache"
