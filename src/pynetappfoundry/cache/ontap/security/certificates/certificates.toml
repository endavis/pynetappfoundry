[endpoint]
path = "/security/certificates"
schema = "security_certificate"
class_name = "OntapSecurityCertificate"

[fields.authority_key_identifier]
cache_strategy = "cache"

[fields."azure.client_certificate"]
cache_strategy = "cache"

[fields."azure.client_id"]
cache_strategy = "cache"

[fields."azure.client_secret"]
cache_strategy = "cache"

[fields."azure.key_vault"]
cache_strategy = "cache"

[fields."azure.oauth_host"]
cache_strategy = "cache"

[fields."azure.proxy.host"]
cache_strategy = "cache"

[fields."azure.proxy.password"]
cache_strategy = "cache"

[fields."azure.proxy.port"]
cache_strategy = "cache"

[fields."azure.proxy.type_"]
cache_strategy = "cache"

[fields."azure.proxy.username"]
cache_strategy = "cache"

[fields."azure.tenant_id"]
cache_strategy = "cache"

[fields."azure.timeout"]
cache_strategy = "cache"

[fields."azure.verify_host"]
cache_strategy = "cache"

[fields.ca]
cache_strategy = "cache"

[fields.common_name]
cache_strategy = "cache"

[fields.expiry_time]
cache_strategy = "cache"

[fields.hash_function]
cache_strategy = "cache"

[fields.intermediate_certificates]
cache_strategy = "cache"

[fields.key_size]
cache_strategy = "cache"

[fields.name]
cache_strategy = "cache"

[fields.private_key]
cache_strategy = "cache"

[fields.public_certificate]
cache_strategy = "cache"

[fields.scope]
cache_strategy = "cache"

[fields.serial_number]
cache_strategy = "cache"

[fields."subject_alternatives.dns"]
cache_strategy = "cache"

[fields."subject_alternatives.email"]
cache_strategy = "cache"

[fields."subject_alternatives.ip"]
cache_strategy = "cache"

[fields."subject_alternatives.uri"]
cache_strategy = "cache"

[fields.subject_key_identifier]
cache_strategy = "cache"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"

[fields.type_]
cache_strategy = "cache"

[fields.uuid]
cache_strategy = "cache"
