[endpoint]
path = "/security/accounts"
schema = "account"
class_name = "OntapAccount"

[fields.applications]
cache_strategy = "cache"

[fields.comment]
cache_strategy = "cache"

[fields.locked]
cache_strategy = "cache"

[fields.name]
cache_strategy = "cache"

[fields."owner.name"]
cache_strategy = "cache"

[fields."owner.uuid"]
cache_strategy = "cache"

[fields.password]
cache_strategy = "cache"

[fields.password_hash_algorithm]
cache_strategy = "cache"

[fields."role.name"]
cache_strategy = "cache"

[fields.scope]
cache_strategy = "cache"
