[endpoint]
path = "/security/anti-ransomware/suspects"
schema = "anti_ransomware_suspect"
class_name = "OntapAntiRansomwareSuspect"

[fields."file.format"]
cache_strategy = "cache"

[fields."file.name"]
cache_strategy = "cache"

[fields."file.path"]
cache_strategy = "cache"

[fields."file.reason"]
cache_strategy = "cache"

[fields."file.suspect_time"]
cache_strategy = "cache"

[fields."volume.name"]
cache_strategy = "cache"

[fields."volume.uuid"]
cache_strategy = "cache"
