[endpoint]
path = "/security/ipsec/security-associations"
schema = "security_association_response"
class_name = "OntapSecurityAssociationResponse"

[fields.cipher_suite]
cache_strategy = "cache"

[fields."ike.authentication"]
cache_strategy = "cache"

[fields."ike.initiator_security_parameter_index"]
cache_strategy = "cache"

[fields."ike.is_initiator"]
cache_strategy = "cache"

[fields."ike.responder_security_parameter_index"]
cache_strategy = "cache"

[fields."ike.state"]
cache_strategy = "cache"

[fields."ike.version"]
cache_strategy = "cache"

[fields."ipsec.action"]
cache_strategy = "cache"

[fields."ipsec.inbound.bytes"]
cache_strategy = "cache"

[fields."ipsec.inbound.offload_bytes"]
cache_strategy = "cache"

[fields."ipsec.inbound.offload_packets"]
cache_strategy = "cache"

[fields."ipsec.inbound.packets"]
cache_strategy = "cache"

[fields."ipsec.inbound.security_parameter_index"]
cache_strategy = "cache"

[fields."ipsec.outbound.bytes"]
cache_strategy = "cache"

[fields."ipsec.outbound.offload_bytes"]
cache_strategy = "cache"

[fields."ipsec.outbound.offload_packets"]
cache_strategy = "cache"

[fields."ipsec.outbound.packets"]
cache_strategy = "cache"

[fields."ipsec.outbound.security_parameter_index"]
cache_strategy = "cache"

[fields."ipsec.state"]
cache_strategy = "cache"

[fields.lifetime]
cache_strategy = "cache"

[fields.local_address]
cache_strategy = "cache"

[fields."node.name"]
cache_strategy = "cache"

[fields."node.uuid"]
cache_strategy = "cache"

[fields.policy_name]
cache_strategy = "cache"

[fields.remote_address]
cache_strategy = "cache"

[fields.scope]
cache_strategy = "cache"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"

[fields.type_]
cache_strategy = "cache"

[fields.uuid]
cache_strategy = "cache"
