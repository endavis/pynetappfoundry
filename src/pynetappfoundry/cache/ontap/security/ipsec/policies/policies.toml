[endpoint]
path = "/security/ipsec/policies"
schema = "ipsec_policy_response"
class_name = "OntapIpsecPolicyResponse"

[fields.action]
cache_strategy = "cache"

[fields.authentication_method]
cache_strategy = "cache"

[fields."certificate.name"]
cache_strategy = "cache"

[fields."certificate.uuid"]
cache_strategy = "cache"

[fields.enabled]
cache_strategy = "cache"

[fields."ipspace.name"]
cache_strategy = "cache"

[fields."ipspace.uuid"]
cache_strategy = "cache"

[fields."local_endpoint.address"]
cache_strategy = "cache"

[fields."local_endpoint.family"]
cache_strategy = "cache"

[fields."local_endpoint.netmask"]
cache_strategy = "cache"

[fields."local_endpoint.port"]
cache_strategy = "cache"

[fields.local_identity]
cache_strategy = "cache"

[fields.name]
cache_strategy = "cache"

[fields.protocol]
cache_strategy = "cache"

[fields."remote_endpoint.address"]
cache_strategy = "cache"

[fields."remote_endpoint.family"]
cache_strategy = "cache"

[fields."remote_endpoint.netmask"]
cache_strategy = "cache"

[fields."remote_endpoint.port"]
cache_strategy = "cache"

[fields.remote_identity]
cache_strategy = "cache"

[fields.scope]
cache_strategy = "cache"

[fields.secret_key]
cache_strategy = "cache"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"

[fields.uuid]
cache_strategy = "cache"
