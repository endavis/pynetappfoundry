[endpoint]
path = "/network/ethernet/switches"
schema = "switch"
class_name = "OntapSwitch"

[fields.address]
cache_strategy = "cache"

[fields.discovered]
cache_strategy = "cache"

[fields.model_]
cache_strategy = "cache"

[fields."monitoring.enabled"]
cache_strategy = "cache"

[fields."monitoring.monitored"]
cache_strategy = "cache"

[fields."monitoring.reason"]
cache_strategy = "cache"

[fields.name]
cache_strategy = "cache"

[fields.network]
cache_strategy = "cache"

[fields.serial_number]
cache_strategy = "cache"

[fields."snmp.user"]
cache_strategy = "cache"

[fields."snmp.version"]
cache_strategy = "cache"

[fields.version]
cache_strategy = "cache"
