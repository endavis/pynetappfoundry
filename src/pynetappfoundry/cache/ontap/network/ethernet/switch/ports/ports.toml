[endpoint]
path = "/network/ethernet/switch/ports"
schema = "switch_port"
class_name = "OntapSwitchPort"

[fields.configured]
cache_strategy = "cache"

[fields.duplex_type]
cache_strategy = "cache"

[fields."identity.index"]
cache_strategy = "cache"

[fields."identity.name"]
cache_strategy = "cache"

[fields."identity.number"]
cache_strategy = "cache"

[fields.isl]
cache_strategy = "cache"

[fields.mac_address]
cache_strategy = "cache"

[fields.mtu]
cache_strategy = "cache"

[fields."remote_port.device.node.name"]
cache_strategy = "cache"

[fields."remote_port.device.node.uuid"]
cache_strategy = "cache"

[fields."remote_port.device.shelf.module"]
cache_strategy = "cache"

[fields."remote_port.device.shelf.name"]
cache_strategy = "cache"

[fields."remote_port.device.shelf.uid"]
cache_strategy = "cache"

[fields."remote_port.mtu"]
cache_strategy = "cache"

[fields."remote_port.name"]
cache_strategy = "cache"

[fields.speed]
cache_strategy = "cache"

[fields.state]
cache_strategy = "cache"

[fields."statistics.receive_raw.discards"]
cache_strategy = "realtime"

[fields."statistics.receive_raw.errors"]
cache_strategy = "realtime"

[fields."statistics.receive_raw.packets"]
cache_strategy = "realtime"

[fields."statistics.transmit_raw.discards"]
cache_strategy = "realtime"

[fields."statistics.transmit_raw.errors"]
cache_strategy = "realtime"

[fields."statistics.transmit_raw.packets"]
cache_strategy = "realtime"

[fields."switch.name"]
cache_strategy = "cache"

[fields.type_]
cache_strategy = "cache"

[fields.vlan_id]
cache_strategy = "cache"

[fields.vpc_peer_link]
cache_strategy = "cache"
