[endpoint]
path = "/network/ethernet/broadcast-domains"
schema = "broadcast_domain"
class_name = "OntapBroadcastDomain"

[fields."ipspace.name"]
cache_strategy = "cache"

[fields."ipspace.uuid"]
cache_strategy = "cache"

[fields.mtu]
cache_strategy = "cache"

[fields.name]
cache_strategy = "cache"

[fields.ports]
cache_strategy = "cache"

[fields.uuid]
cache_strategy = "cache"
