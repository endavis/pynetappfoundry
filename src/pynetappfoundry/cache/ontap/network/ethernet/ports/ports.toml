[endpoint]
path = "/network/ethernet/ports"
schema = "port"
class_name = "OntapPort"

[fields."broadcast_domain.ipspace.name"]
cache_strategy = "cache"

[fields."broadcast_domain.name"]
cache_strategy = "cache"

[fields."broadcast_domain.uuid"]
cache_strategy = "cache"

[fields.discovered_devices]
cache_strategy = "cache"

[fields.enabled]
cache_strategy = "cache"

[fields.flowcontrol_admin]
cache_strategy = "cache"

[fields.interface_count]
cache_strategy = "cache"

[fields."lag.active_ports"]
cache_strategy = "cache"

[fields."lag.distribution_policy"]
cache_strategy = "cache"

[fields."lag.member_ports"]
cache_strategy = "cache"

[fields."lag.mode"]
cache_strategy = "cache"

[fields.mac_address]
cache_strategy = "cache"

[fields."metric.duration"]
cache_strategy = "realtime"

[fields."metric.status"]
cache_strategy = "realtime"

[fields."metric.throughput.read"]
cache_strategy = "realtime"

[fields."metric.throughput.total"]
cache_strategy = "realtime"

[fields."metric.throughput.write"]
cache_strategy = "realtime"

[fields."metric.timestamp"]
cache_strategy = "realtime"

[fields.mtu]
cache_strategy = "cache"

[fields.name]
cache_strategy = "cache"

[fields."node.name"]
cache_strategy = "cache"

[fields."node.uuid"]
cache_strategy = "cache"

[fields.pfc_queues_admin]
cache_strategy = "cache"

[fields.rdma_protocols]
cache_strategy = "cache"

[fields.reachability]
cache_strategy = "cache"

[fields.reachable_broadcast_domains]
cache_strategy = "cache"

[fields.speed]
cache_strategy = "cache"

[fields.state]
cache_strategy = "cache"

[fields."statistics.device.link_down_count_raw"]
cache_strategy = "realtime"

[fields."statistics.device.receive_raw.discards"]
cache_strategy = "realtime"

[fields."statistics.device.receive_raw.errors"]
cache_strategy = "realtime"

[fields."statistics.device.receive_raw.packets"]
cache_strategy = "realtime"

[fields."statistics.device.timestamp"]
cache_strategy = "realtime"

[fields."statistics.device.transmit_raw.discards"]
cache_strategy = "realtime"

[fields."statistics.device.transmit_raw.errors"]
cache_strategy = "realtime"

[fields."statistics.device.transmit_raw.packets"]
cache_strategy = "realtime"

[fields."statistics.status"]
cache_strategy = "realtime"

[fields."statistics.throughput_raw.read"]
cache_strategy = "realtime"

[fields."statistics.throughput_raw.total"]
cache_strategy = "realtime"

[fields."statistics.throughput_raw.write"]
cache_strategy = "realtime"

[fields."statistics.timestamp"]
cache_strategy = "realtime"

[fields.type_]
cache_strategy = "cache"

[fields.uuid]
cache_strategy = "cache"

[fields."vlan.base_port.name"]
cache_strategy = "cache"

[fields."vlan.base_port.node.name"]
cache_strategy = "cache"

[fields."vlan.base_port.uuid"]
cache_strategy = "cache"

[fields."vlan.tag"]
cache_strategy = "cache"
