[endpoint]
path = "/network/http-proxy"
schema = "network_http_proxy"
class_name = "OntapNetworkHttpProxy"

[fields.authentication_enabled]
cache_strategy = "cache"

[fields."ipspace.name"]
cache_strategy = "cache"

[fields."ipspace.uuid"]
cache_strategy = "cache"

[fields.password]
cache_strategy = "cache"

[fields.port]
cache_strategy = "cache"

[fields.scope]
cache_strategy = "cache"

[fields.server]
cache_strategy = "cache"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"

[fields.username]
cache_strategy = "cache"

[fields.uuid]
cache_strategy = "cache"
