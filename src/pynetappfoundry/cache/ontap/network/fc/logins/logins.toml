[endpoint]
path = "/network/fc/logins"
schema = "fc_login"
class_name = "OntapFcLogin"

[fields.igroups]
cache_strategy = "cache"

[fields."initiator.aliases"]
cache_strategy = "cache"

[fields."initiator.comment"]
cache_strategy = "cache"

[fields."initiator.port_address"]
cache_strategy = "cache"

[fields."initiator.wwnn"]
cache_strategy = "cache"

[fields."initiator.wwpn"]
cache_strategy = "cache"

[fields."interface.name"]
cache_strategy = "cache"

[fields."interface.uuid"]
cache_strategy = "cache"

[fields."interface.wwpn"]
cache_strategy = "cache"

[fields.protocol]
cache_strategy = "cache"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"
