[endpoint]
path = "/network/fc/interfaces/{fc_interface.uuid}/metrics"
schema = "performance_fc_interface_metric_response"
class_name = "OntapPerformanceFcInterfaceMetricResponse"

[fields.duration]
cache_strategy = "cache"

[fields."iops.other"]
cache_strategy = "cache"

[fields."iops.read"]
cache_strategy = "cache"

[fields."iops.total"]
cache_strategy = "cache"

[fields."iops.write"]
cache_strategy = "cache"

[fields."latency.other"]
cache_strategy = "cache"

[fields."latency.read"]
cache_strategy = "cache"

[fields."latency.total"]
cache_strategy = "cache"

[fields."latency.write"]
cache_strategy = "cache"

[fields.status]
cache_strategy = "cache"

[fields."throughput.read"]
cache_strategy = "cache"

[fields."throughput.total"]
cache_strategy = "cache"

[fields."throughput.write"]
cache_strategy = "cache"

[fields.timestamp]
cache_strategy = "cache"

[fields.uuid]
cache_strategy = "cache"
