[endpoint]
path = "/network/ip/subnets"
schema = "ip_subnet"
class_name = "OntapIpSubnet"

[fields.available_count]
cache_strategy = "cache"

[fields.available_ip_ranges]
cache_strategy = "cache"

[fields."broadcast_domain.name"]
cache_strategy = "cache"

[fields."broadcast_domain.uuid"]
cache_strategy = "cache"

[fields.fail_if_lifs_conflict]
cache_strategy = "cache"

[fields.gateway]
cache_strategy = "cache"

[fields.ip_ranges]
cache_strategy = "cache"

[fields."ipspace.name"]
cache_strategy = "cache"

[fields."ipspace.uuid"]
cache_strategy = "cache"

[fields.name]
cache_strategy = "cache"

[fields."subnet.address"]
cache_strategy = "cache"

[fields."subnet.family"]
cache_strategy = "cache"

[fields."subnet.netmask"]
cache_strategy = "cache"

[fields.total_count]
cache_strategy = "cache"

[fields.used_count]
cache_strategy = "cache"

[fields.uuid]
cache_strategy = "cache"
