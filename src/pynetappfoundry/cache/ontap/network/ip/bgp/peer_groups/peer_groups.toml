[endpoint]
path = "/network/ip/bgp/peer-groups"
schema = "bgp_peer_group"
class_name = "OntapBgpPeerGroup"

[fields."ipspace.name"]
cache_strategy = "cache"

[fields."ipspace.uuid"]
cache_strategy = "cache"

[fields."local.interface.ip.address"]
cache_strategy = "cache"

[fields."local.interface.name"]
cache_strategy = "cache"

[fields."local.interface.uuid"]
cache_strategy = "cache"

[fields."local.ip.address"]
cache_strategy = "cache"

[fields."local.ip.netmask"]
cache_strategy = "cache"

[fields."local.port.name"]
cache_strategy = "cache"

[fields."local.port.node.name"]
cache_strategy = "cache"

[fields."local.port.uuid"]
cache_strategy = "cache"

[fields.name]
cache_strategy = "cache"

[fields."peer.address"]
cache_strategy = "cache"

[fields."peer.asn"]
cache_strategy = "cache"

[fields."peer.is_next_hop"]
cache_strategy = "cache"

[fields."peer.md5_enabled"]
cache_strategy = "cache"

[fields."peer.md5_secret"]
cache_strategy = "cache"

[fields.state]
cache_strategy = "cache"

[fields.uuid]
cache_strategy = "cache"
