[endpoint]
path = "/cluster/web"
schema = "web"
class_name = "OntapWeb"

[fields."certificate.name"]
cache_strategy = "cache"

[fields."certificate.uuid"]
cache_strategy = "cache"

[fields.client_enabled]
cache_strategy = "cache"

[fields."csrf.protection_enabled"]
cache_strategy = "cache"

[fields."csrf.token.concurrent_limit"]
cache_strategy = "cache"

[fields."csrf.token.idle_timeout"]
cache_strategy = "cache"

[fields."csrf.token.max_timeout"]
cache_strategy = "cache"

[fields.enabled]
cache_strategy = "cache"

[fields.http_enabled]
cache_strategy = "cache"

[fields.http_port]
cache_strategy = "cache"

[fields.https_port]
cache_strategy = "cache"

[fields.ocsp_enabled]
cache_strategy = "cache"

[fields.per_address_limit]
cache_strategy = "cache"

[fields.state]
cache_strategy = "cache"

[fields.wait_queue_capacity]
cache_strategy = "cache"
