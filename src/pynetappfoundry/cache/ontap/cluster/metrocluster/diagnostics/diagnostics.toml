[endpoint]
path = "/cluster/metrocluster/diagnostics"
schema = "metrocluster_diagnostics"
class_name = "OntapMetroclusterDiagnostics"

[fields."aggregate.details"]
cache_strategy = "cache"

[fields."aggregate.state"]
cache_strategy = "cache"

[fields."aggregate.summary.code"]
cache_strategy = "cache"

[fields."aggregate.summary.message"]
cache_strategy = "cache"

[fields."aggregate.timestamp"]
cache_strategy = "cache"

[fields."cluster.details"]
cache_strategy = "cache"

[fields."cluster.state"]
cache_strategy = "cache"

[fields."cluster.summary.code"]
cache_strategy = "cache"

[fields."cluster.summary.message"]
cache_strategy = "cache"

[fields."cluster.timestamp"]
cache_strategy = "cache"

[fields."config_replication.state"]
cache_strategy = "cache"

[fields."config_replication.summary.code"]
cache_strategy = "cache"

[fields."config_replication.summary.message"]
cache_strategy = "cache"

[fields."config_replication.timestamp"]
cache_strategy = "cache"

[fields."connection.details"]
cache_strategy = "cache"

[fields."connection.state"]
cache_strategy = "cache"

[fields."connection.summary.code"]
cache_strategy = "cache"

[fields."connection.summary.message"]
cache_strategy = "cache"

[fields."connection.timestamp"]
cache_strategy = "cache"

[fields."interface.state"]
cache_strategy = "cache"

[fields."interface.summary.code"]
cache_strategy = "cache"

[fields."interface.summary.message"]
cache_strategy = "cache"

[fields."interface.timestamp"]
cache_strategy = "cache"

[fields."node.details"]
cache_strategy = "cache"

[fields."node.state"]
cache_strategy = "cache"

[fields."node.summary.code"]
cache_strategy = "cache"

[fields."node.summary.message"]
cache_strategy = "cache"

[fields."node.timestamp"]
cache_strategy = "cache"

[fields."volume.details"]
cache_strategy = "cache"

[fields."volume.state"]
cache_strategy = "cache"

[fields."volume.summary.code"]
cache_strategy = "cache"

[fields."volume.summary.message"]
cache_strategy = "cache"

[fields."volume.timestamp"]
cache_strategy = "cache"
