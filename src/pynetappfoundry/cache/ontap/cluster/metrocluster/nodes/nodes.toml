[endpoint]
path = "/cluster/metrocluster/nodes"
schema = "metrocluster_node"
class_name = "OntapMetroclusterNode"

[fields.automatic_uso]
cache_strategy = "cache"

[fields."cluster.name"]
cache_strategy = "cache"

[fields."cluster.uuid"]
cache_strategy = "cache"

[fields.configuration_state]
cache_strategy = "cache"

[fields."dr_auxiliary_cluster.name"]
cache_strategy = "cache"

[fields."dr_auxiliary_cluster.uuid"]
cache_strategy = "cache"

[fields."dr_auxiliary_partner.name"]
cache_strategy = "cache"

[fields."dr_auxiliary_partner.system_id"]
cache_strategy = "cache"

[fields."dr_auxiliary_partner.uuid"]
cache_strategy = "cache"

[fields.dr_group_id]
cache_strategy = "cache"

[fields.dr_mirroring_state]
cache_strategy = "cache"

[fields.dr_operation_state]
cache_strategy = "cache"

[fields."dr_partner.name"]
cache_strategy = "cache"

[fields."dr_partner.system_id"]
cache_strategy = "cache"

[fields."dr_partner.uuid"]
cache_strategy = "cache"

[fields."dr_partner_cluster.name"]
cache_strategy = "cache"

[fields."dr_partner_cluster.uuid"]
cache_strategy = "cache"

[fields.encryption_enabled]
cache_strategy = "cache"

[fields."ha_partner.name"]
cache_strategy = "cache"

[fields."ha_partner.system_id"]
cache_strategy = "cache"

[fields."ha_partner.uuid"]
cache_strategy = "cache"

[fields."ha_partner_cluster.name"]
cache_strategy = "cache"

[fields."ha_partner_cluster.uuid"]
cache_strategy = "cache"

[fields.is_mccip]
cache_strategy = "cache"

[fields.limit_enforcement]
cache_strategy = "cache"

[fields."node.name"]
cache_strategy = "cache"

[fields."node.system_id"]
cache_strategy = "cache"

[fields."node.uuid"]
cache_strategy = "cache"
