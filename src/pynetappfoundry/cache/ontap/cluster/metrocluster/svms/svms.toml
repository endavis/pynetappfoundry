[endpoint]
path = "/cluster/metrocluster/svms"
schema = "metrocluster_svm"
class_name = "OntapMetroclusterSvm"

[fields."cluster.name"]
cache_strategy = "cache"

[fields."cluster.uuid"]
cache_strategy = "cache"

[fields.configuration_state]
cache_strategy = "cache"

[fields."failed_reason.arguments"]
cache_strategy = "cache"

[fields."failed_reason.code"]
cache_strategy = "cache"

[fields."failed_reason.message"]
cache_strategy = "cache"

[fields."partner_svm.name"]
cache_strategy = "cache"

[fields."partner_svm.uuid"]
cache_strategy = "cache"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"
