[endpoint]
path = "/cluster/metrocluster/dr-groups"
schema = "metrocluster_dr_group"
class_name = "OntapMetroclusterDrGroup"

[fields.dr_pairs]
cache_strategy = "cache"

[fields.id]
cache_strategy = "cache"

[fields.mccip_ports]
cache_strategy = "cache"

[fields."partner_cluster.name"]
cache_strategy = "cache"

[fields."partner_cluster.uuid"]
cache_strategy = "cache"
