[endpoint]
path = "/cluster/metrocluster/interconnects"
schema = "metrocluster_interconnect"
class_name = "OntapMetroclusterInterconnect"

[fields.adapter]
cache_strategy = "cache"

[fields.interfaces]
cache_strategy = "cache"

[fields."mirror.enabled"]
cache_strategy = "cache"

[fields."mirror.state"]
cache_strategy = "cache"

[fields.multipath_policy]
cache_strategy = "cache"

[fields."node.name"]
cache_strategy = "cache"

[fields."node.uuid"]
cache_strategy = "cache"

[fields.partner_type]
cache_strategy = "cache"

[fields.state]
cache_strategy = "cache"

[fields.type_]
cache_strategy = "cache"

[fields.vlan_id]
cache_strategy = "cache"
