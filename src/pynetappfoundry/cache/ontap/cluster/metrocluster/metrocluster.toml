[endpoint]
path = "/cluster/metrocluster"
schema = "metrocluster"
class_name = "OntapMetrocluster"

[fields."node.name"]
cache_strategy = "cache"

[fields."node.uuid"]
cache_strategy = "cache"

[fields."partner.name"]
cache_strategy = "cache"

[fields."partner.uuid"]
cache_strategy = "cache"
