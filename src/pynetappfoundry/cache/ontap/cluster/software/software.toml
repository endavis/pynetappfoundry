[endpoint]
path = "/cluster/software"
schema = "software_reference"
class_name = "OntapSoftwareReference"

[fields."firmware.cluster_fw_progress"]
cache_strategy = "cache"

[fields."firmware.disk.average_duration_per_disk"]
cache_strategy = "cache"

[fields."firmware.disk.num_waiting_download"]
cache_strategy = "cache"

[fields."firmware.disk.total_completion_estimate"]
cache_strategy = "cache"

[fields."firmware.disk.update_status"]
cache_strategy = "cache"

[fields."firmware.dqp.file_name"]
cache_strategy = "cache"

[fields."firmware.dqp.record_count.alias"]
cache_strategy = "cache"

[fields."firmware.dqp.record_count.device"]
cache_strategy = "cache"

[fields."firmware.dqp.record_count.drive"]
cache_strategy = "cache"

[fields."firmware.dqp.record_count.system"]
cache_strategy = "cache"

[fields."firmware.dqp.revision"]
cache_strategy = "cache"

[fields."firmware.dqp.version"]
cache_strategy = "cache"

[fields."firmware.shelf.in_progress_count"]
cache_strategy = "cache"

[fields."firmware.shelf.update_status"]
cache_strategy = "cache"

[fields."firmware.sp_bmc.autoupdate"]
cache_strategy = "cache"

[fields."firmware.sp_bmc.end_time"]
cache_strategy = "cache"

[fields."firmware.sp_bmc.fw_type"]
cache_strategy = "cache"

[fields."firmware.sp_bmc.image"]
cache_strategy = "cache"

[fields."firmware.sp_bmc.in_progress"]
cache_strategy = "cache"

[fields."firmware.sp_bmc.is_current"]
cache_strategy = "cache"

[fields."firmware.sp_bmc.last_update_state"]
cache_strategy = "cache"

[fields."firmware.sp_bmc.percent_done"]
cache_strategy = "cache"

[fields."firmware.sp_bmc.running_version"]
cache_strategy = "cache"

[fields."firmware.sp_bmc.start_time"]
cache_strategy = "cache"

[fields."firmware.sp_bmc.state"]
cache_strategy = "cache"

[fields.name]
cache_strategy = "cache"

[fields.software_images]
cache_strategy = "cache"

[fields.version]
cache_strategy = "cache"
