[endpoint]
path = "/cluster/peers"
schema = "cluster_peer"
class_name = "OntapClusterPeer"

[fields."authentication.expiry_time"]
cache_strategy = "cache"

[fields."authentication.generate_passphrase"]
cache_strategy = "cache"

[fields."authentication.in_use"]
cache_strategy = "cache"

[fields."authentication.passphrase"]
cache_strategy = "cache"

[fields."authentication.state"]
cache_strategy = "cache"

[fields."encryption.proposed"]
cache_strategy = "cache"

[fields."encryption.state"]
cache_strategy = "cache"

[fields.initial_allowed_svms]
cache_strategy = "cache"

[fields.ip_address]
cache_strategy = "cache"

[fields."ipspace.name"]
cache_strategy = "cache"

[fields."ipspace.uuid"]
cache_strategy = "cache"

[fields."local_network.broadcast_domain"]
cache_strategy = "cache"

[fields."local_network.gateway"]
cache_strategy = "cache"

[fields."local_network.interfaces"]
cache_strategy = "cache"

[fields."local_network.netmask"]
cache_strategy = "cache"

[fields.name]
cache_strategy = "cache"

[fields.peer_applications]
cache_strategy = "cache"

[fields."remote.ip_addresses"]
cache_strategy = "cache"

[fields."remote.name"]
cache_strategy = "cache"

[fields."remote.serial_number"]
cache_strategy = "cache"

[fields."status.state"]
cache_strategy = "cache"

[fields."status.update_time"]
cache_strategy = "cache"

[fields.uuid]
cache_strategy = "cache"

[fields."version.full"]
cache_strategy = "cache"

[fields."version.generation"]
cache_strategy = "cache"

[fields."version.major"]
cache_strategy = "cache"

[fields."version.minor"]
cache_strategy = "cache"
