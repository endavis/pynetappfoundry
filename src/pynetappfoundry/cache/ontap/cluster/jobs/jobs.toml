[endpoint]
path = "/cluster/jobs"
schema = "job"
class_name = "OntapJob"

[fields.code]
cache_strategy = "cache"

[fields.description]
cache_strategy = "cache"

[fields.end_time]
cache_strategy = "cache"

[fields."error.arguments"]
cache_strategy = "cache"

[fields."error.code"]
cache_strategy = "cache"

[fields."error.message"]
cache_strategy = "cache"

[fields.message]
cache_strategy = "cache"

[fields."node.name"]
cache_strategy = "cache"

[fields.start_time]
cache_strategy = "cache"

[fields.state]
cache_strategy = "cache"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"

[fields.uuid]
cache_strategy = "cache"
