[endpoint]
path = "/cluster/nodes"
schema = "node_response"
class_name = "OntapNodeResponse"

[fields.anti_ransomware_version]
cache_strategy = "cache"

[fields."cluster_interface.ip.address"]
cache_strategy = "cache"

[fields.cluster_interfaces]
cache_strategy = "cache"

[fields."controller.board"]
cache_strategy = "cache"

[fields."controller.cpu.count"]
cache_strategy = "cache"

[fields."controller.cpu.firmware_release"]
cache_strategy = "cache"

[fields."controller.cpu.processor"]
cache_strategy = "cache"

[fields."controller.failed_fan.count"]
cache_strategy = "cache"

[fields."controller.failed_fan.message.code"]
cache_strategy = "cache"

[fields."controller.failed_fan.message.message"]
cache_strategy = "cache"

[fields."controller.failed_power_supply.count"]
cache_strategy = "cache"

[fields."controller.failed_power_supply.message.code"]
cache_strategy = "cache"

[fields."controller.failed_power_supply.message.message"]
cache_strategy = "cache"

[fields."controller.flash_cache"]
cache_strategy = "cache"

[fields."controller.frus"]
cache_strategy = "cache"

[fields."controller.memory_size"]
cache_strategy = "cache"

[fields."controller.over_temperature"]
cache_strategy = "cache"

[fields.date]
cache_strategy = "cache"

[fields."external_cache.is_enabled"]
cache_strategy = "cache"

[fields."external_cache.is_hya_enabled"]
cache_strategy = "cache"

[fields."external_cache.is_rewarm_enabled"]
cache_strategy = "cache"

[fields."external_cache.pcs_size"]
cache_strategy = "cache"

[fields."ha.auto_giveback"]
cache_strategy = "cache"

[fields."ha.enabled"]
cache_strategy = "cache"

[fields."ha.giveback.failure.code"]
cache_strategy = "cache"

[fields."ha.giveback.failure.message"]
cache_strategy = "cache"

[fields."ha.giveback.state"]
cache_strategy = "cache"

[fields."ha.giveback.status"]
cache_strategy = "cache"

[fields."ha.interconnect.adapter"]
cache_strategy = "cache"

[fields."ha.interconnect.state"]
cache_strategy = "cache"

[fields."ha.partners"]
cache_strategy = "cache"

[fields."ha.ports"]
cache_strategy = "cache"

[fields."ha.takeover.failure.code"]
cache_strategy = "cache"

[fields."ha.takeover.failure.message"]
cache_strategy = "cache"

[fields."ha.takeover.state"]
cache_strategy = "cache"

[fields."ha.takeover_check.reasons"]
cache_strategy = "cache"

[fields."ha.takeover_check.takeover_possible"]
cache_strategy = "cache"

[fields."hw_assist.status.enabled"]
cache_strategy = "cache"

[fields."hw_assist.status.local.ip"]
cache_strategy = "cache"

[fields."hw_assist.status.local.port"]
cache_strategy = "cache"

[fields."hw_assist.status.local.state"]
cache_strategy = "cache"

[fields."hw_assist.status.partner.ip"]
cache_strategy = "cache"

[fields."hw_assist.status.partner.port"]
cache_strategy = "cache"

[fields."hw_assist.status.partner.state"]
cache_strategy = "cache"

[fields.is_spares_low]
cache_strategy = "cache"

[fields.location]
cache_strategy = "cache"

[fields."management_interface.ip.address"]
cache_strategy = "cache"

[fields.management_interfaces]
cache_strategy = "cache"

[fields.membership]
cache_strategy = "cache"

[fields."metric.duration"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.processor_utilization"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.status"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.timestamp"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.uuid"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metrocluster.custom_vlan_capable"]
cache_strategy = "cache"

[fields."metrocluster.ports"]
cache_strategy = "cache"

[fields."metrocluster.type_"]
cache_strategy = "cache"

[fields.model_]
cache_strategy = "cache"

[fields.name]
cache_strategy = "cache"

[fields."nvram.battery_state"]
cache_strategy = "cache"

[fields."nvram.id"]
cache_strategy = "cache"

[fields.owner]
cache_strategy = "cache"

[fields.serial_number]
cache_strategy = "cache"

[fields."service_processor.api_service.enabled"]
cache_strategy = "cache"

[fields."service_processor.api_service.limit_access"]
cache_strategy = "cache"

[fields."service_processor.api_service.port"]
cache_strategy = "cache"

[fields."service_processor.auto_config.ipv4_subnet"]
cache_strategy = "cache"

[fields."service_processor.auto_config.ipv6_subnet"]
cache_strategy = "cache"

[fields."service_processor.autoupdate_enabled"]
cache_strategy = "cache"

[fields."service_processor.backup.is_current"]
cache_strategy = "cache"

[fields."service_processor.backup.state"]
cache_strategy = "cache"

[fields."service_processor.backup.version"]
cache_strategy = "cache"

[fields."service_processor.dhcp_enabled"]
cache_strategy = "cache"

[fields."service_processor.firmware_version"]
cache_strategy = "cache"

[fields."service_processor.ipv4_interface.address"]
cache_strategy = "cache"

[fields."service_processor.ipv4_interface.enabled"]
cache_strategy = "cache"

[fields."service_processor.ipv4_interface.gateway"]
cache_strategy = "cache"

[fields."service_processor.ipv4_interface.netmask"]
cache_strategy = "cache"

[fields."service_processor.ipv4_interface.setup_state"]
cache_strategy = "cache"

[fields."service_processor.ipv6_interface.address"]
cache_strategy = "cache"

[fields."service_processor.ipv6_interface.enabled"]
cache_strategy = "cache"

[fields."service_processor.ipv6_interface.gateway"]
cache_strategy = "cache"

[fields."service_processor.ipv6_interface.is_ipv6_ra_enabled"]
cache_strategy = "cache"

[fields."service_processor.ipv6_interface.link_local_ip"]
cache_strategy = "cache"

[fields."service_processor.ipv6_interface.netmask"]
cache_strategy = "cache"

[fields."service_processor.ipv6_interface.router_ip"]
cache_strategy = "cache"

[fields."service_processor.ipv6_interface.setup_state"]
cache_strategy = "cache"

[fields."service_processor.is_ip_configured"]
cache_strategy = "cache"

[fields."service_processor.last_update_state"]
cache_strategy = "cache"

[fields."service_processor.link_status"]
cache_strategy = "cache"

[fields."service_processor.mac_address"]
cache_strategy = "cache"

[fields."service_processor.primary.is_current"]
cache_strategy = "cache"

[fields."service_processor.primary.state"]
cache_strategy = "cache"

[fields."service_processor.primary.version"]
cache_strategy = "cache"

[fields."service_processor.ssh_info.allowed_addresses"]
cache_strategy = "cache"

[fields."service_processor.state"]
cache_strategy = "cache"

[fields."service_processor.type_"]
cache_strategy = "cache"

[fields."service_processor.web_service.enabled"]
cache_strategy = "cache"

[fields."service_processor.web_service.limit_access"]
cache_strategy = "cache"

[fields."snaplock.compliance_clock_time"]
cache_strategy = "cache"

[fields.state]
cache_strategy = "cache"

[fields."statistics.processor_utilization_base"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."statistics.processor_utilization_raw"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."statistics.status"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."statistics.timestamp"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields.storage_configuration]
cache_strategy = "cache"

[fields."system_aggregate.name"]
cache_strategy = "cache"

[fields."system_aggregate.uuid"]
cache_strategy = "cache"

[fields.system_id]
cache_strategy = "cache"

[fields.system_machine_type]
cache_strategy = "cache"

[fields.uptime]
cache_strategy = "realtime"

[fields.uuid]
cache_strategy = "cache"

[fields.vendor_serial_number]
cache_strategy = "cache"

[fields."version.full"]
cache_strategy = "cache"

[fields."version.generation"]
cache_strategy = "cache"

[fields."version.major"]
cache_strategy = "cache"

[fields."version.minor"]
cache_strategy = "cache"

[fields."vm.provider_type"]
cache_strategy = "cache"
