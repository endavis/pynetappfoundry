[endpoint]
path = "/storage/pools"
schema = "storage_pool"
class_name = "OntapStoragePool"

[fields."capacity.disk_count"]
cache_strategy = "cache"

[fields."capacity.disks"]
cache_strategy = "cache"

[fields."capacity.remaining"]
cache_strategy = "cache"

[fields."capacity.spare_allocation_units"]
cache_strategy = "cache"

[fields."capacity.total"]
cache_strategy = "cache"

[fields."capacity.used_allocation_units"]
cache_strategy = "cache"

[fields."health.is_healthy"]
cache_strategy = "cache"

[fields."health.state"]
cache_strategy = "cache"

[fields."health.unhealthy_reason.arguments"]
cache_strategy = "cache"

[fields."health.unhealthy_reason.code"]
cache_strategy = "cache"

[fields."health.unhealthy_reason.message"]
cache_strategy = "cache"

[fields.name]
cache_strategy = "cache"

[fields.nodes]
cache_strategy = "cache"

[fields.storage_type]
cache_strategy = "cache"

[fields.uuid]
cache_strategy = "cache"
