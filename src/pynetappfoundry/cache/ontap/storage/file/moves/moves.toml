[endpoint]
path = "/storage/file/moves"
schema = "file_move"
class_name = "OntapFileMove"

[fields.cutover_time]
cache_strategy = "cache"

[fields."destination.path"]
cache_strategy = "cache"

[fields."destination.svm.name"]
cache_strategy = "cache"

[fields."destination.svm.uuid"]
cache_strategy = "cache"

[fields."destination.volume.name"]
cache_strategy = "cache"

[fields."destination.volume.uuid"]
cache_strategy = "cache"

[fields.elapsed_time]
cache_strategy = "cache"

[fields."failure.arguments"]
cache_strategy = "cache"

[fields."failure.code"]
cache_strategy = "cache"

[fields."failure.message"]
cache_strategy = "cache"

[fields."files_to_move.destinations"]
cache_strategy = "cache"

[fields."files_to_move.sources"]
cache_strategy = "cache"

[fields.index]
cache_strategy = "cache"

[fields.is_destination_ready]
cache_strategy = "cache"

[fields.is_flexgroup]
cache_strategy = "cache"

[fields.is_snapshot_fenced]
cache_strategy = "cache"

[fields.max_cutover_time]
cache_strategy = "cache"

[fields.max_throughput]
cache_strategy = "cache"

[fields."node.name"]
cache_strategy = "cache"

[fields."node.uuid"]
cache_strategy = "cache"

[fields."reference.max_cutover_time"]
cache_strategy = "cache"

[fields."reference.path"]
cache_strategy = "cache"

[fields."reference.svm.name"]
cache_strategy = "cache"

[fields."reference.svm.uuid"]
cache_strategy = "cache"

[fields."reference.volume.name"]
cache_strategy = "cache"

[fields."reference.volume.uuid"]
cache_strategy = "cache"

[fields."scanner.percent"]
cache_strategy = "cache"

[fields."scanner.progress"]
cache_strategy = "cache"

[fields."scanner.state"]
cache_strategy = "cache"

[fields."scanner.total"]
cache_strategy = "cache"

[fields."source.path"]
cache_strategy = "cache"

[fields."source.svm.name"]
cache_strategy = "cache"

[fields."source.svm.uuid"]
cache_strategy = "cache"

[fields."source.volume.name"]
cache_strategy = "cache"

[fields."source.volume.uuid"]
cache_strategy = "cache"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"

[fields.uuid]
cache_strategy = "cache"

[fields."volume.name"]
cache_strategy = "cache"

[fields."volume.uuid"]
cache_strategy = "cache"
