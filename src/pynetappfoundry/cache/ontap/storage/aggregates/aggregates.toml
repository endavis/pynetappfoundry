[endpoint]
path = "/storage/aggregates"
schema = "aggregate"
class_name = "OntapAggregate"

[fields."block_storage.hybrid_cache.disk_count"]
cache_strategy = "cache"

[fields."block_storage.hybrid_cache.disk_type"]
cache_strategy = "cache"

[fields."block_storage.hybrid_cache.enabled"]
cache_strategy = "cache"

[fields."block_storage.hybrid_cache.raid_size"]
cache_strategy = "cache"

[fields."block_storage.hybrid_cache.raid_type"]
cache_strategy = "cache"

[fields."block_storage.hybrid_cache.simulated_raid_groups"]
cache_strategy = "cache"

[fields."block_storage.hybrid_cache.size"]
cache_strategy = "cache"

[fields."block_storage.hybrid_cache.storage_pools"]
cache_strategy = "cache"

[fields."block_storage.hybrid_cache.used"]
cache_strategy = "cache"

[fields."block_storage.mirror.enabled"]
cache_strategy = "cache"

[fields."block_storage.mirror.state"]
cache_strategy = "cache"

[fields."block_storage.plexes"]
cache_strategy = "cache"

[fields."block_storage.primary.checksum_style"]
cache_strategy = "cache"

[fields."block_storage.primary.disk_class"]
cache_strategy = "cache"

[fields."block_storage.primary.disk_count"]
cache_strategy = "cache"

[fields."block_storage.primary.disk_type"]
cache_strategy = "cache"

[fields."block_storage.primary.raid_size"]
cache_strategy = "cache"

[fields."block_storage.primary.raid_type"]
cache_strategy = "cache"

[fields."block_storage.primary.simulated_raid_groups"]
cache_strategy = "cache"

[fields."block_storage.storage_type"]
cache_strategy = "cache"

[fields."block_storage.uses_partitions"]
cache_strategy = "cache"

[fields."cloud_storage.attach_eligible"]
cache_strategy = "cache"

[fields."cloud_storage.migrate_threshold"]
cache_strategy = "cache"

[fields."cloud_storage.stores"]
cache_strategy = "cache"

[fields."cloud_storage.tiering_fullness_threshold"]
cache_strategy = "cache"

[fields.create_time]
cache_strategy = "cache"

[fields."data_encryption.drive_protection_enabled"]
cache_strategy = "cache"

[fields."data_encryption.software_encryption_enabled"]
cache_strategy = "cache"

[fields."dr_home_node.name"]
cache_strategy = "cache"

[fields."dr_home_node.uuid"]
cache_strategy = "cache"

[fields."home_node.name"]
cache_strategy = "cache"

[fields."home_node.uuid"]
cache_strategy = "cache"

[fields."inactive_data_reporting.enabled"]
cache_strategy = "cache"

[fields."inactive_data_reporting.start_time"]
cache_strategy = "cache"

[fields."inode_attributes.file_private_capacity"]
cache_strategy = "cache"

[fields."inode_attributes.file_public_capacity"]
cache_strategy = "cache"

[fields."inode_attributes.files_private_used"]
cache_strategy = "cache"

[fields."inode_attributes.files_total"]
cache_strategy = "cache"

[fields."inode_attributes.files_used"]
cache_strategy = "cache"

[fields."inode_attributes.max_files_available"]
cache_strategy = "realtime"

[fields."inode_attributes.max_files_possible"]
cache_strategy = "cache"

[fields."inode_attributes.max_files_used"]
cache_strategy = "cache"

[fields."inode_attributes.used_percent"]
cache_strategy = "cache"

[fields."inode_attributes.version"]
cache_strategy = "cache"

[fields.is_spare_low]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields."metric.duration"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.iops.other"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.iops.read"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.iops.total"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.iops.write"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.latency.other"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.latency.read"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.latency.total"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.latency.write"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.status"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.throughput.other"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.throughput.read"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.throughput.total"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.throughput.write"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.timestamp"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields.name]
cache_strategy = "cache"

[fields."node.name"]
cache_strategy = "cache"

[fields."node.uuid"]
cache_strategy = "cache"

[fields.sidl_enabled]
cache_strategy = "cache"

[fields.snaplock_type]
cache_strategy = "cache"

[fields."snapshot.files_total"]
cache_strategy = "cache"

[fields."snapshot.files_used"]
cache_strategy = "cache"

[fields."snapshot.max_files_available"]
cache_strategy = "cache"

[fields."snapshot.max_files_used"]
cache_strategy = "cache"

[fields."space.block_storage.aggregate_metadata"]
cache_strategy = "realtime"

[fields."space.block_storage.aggregate_metadata_percent"]
cache_strategy = "realtime"

[fields."space.block_storage.available"]
cache_strategy = "realtime"

[fields."space.block_storage.data_compacted_count"]
cache_strategy = "realtime"

[fields."space.block_storage.data_compaction_space_saved"]
cache_strategy = "realtime"

[fields."space.block_storage.data_compaction_space_saved_percent"]
cache_strategy = "realtime"

[fields."space.block_storage.full_threshold_percent"]
cache_strategy = "cache"

[fields."space.block_storage.inactive_user_data"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."space.block_storage.inactive_user_data_percent"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."space.block_storage.physical_used"]
cache_strategy = "realtime"

[fields."space.block_storage.physical_used_percent"]
cache_strategy = "realtime"

[fields."space.block_storage.size"]
cache_strategy = "realtime"

[fields."space.block_storage.used"]
cache_strategy = "realtime"

[fields."space.block_storage.used_including_snapshot_reserve"]
cache_strategy = "realtime"

[fields."space.block_storage.used_including_snapshot_reserve_percent"]
cache_strategy = "realtime"

[fields."space.block_storage.used_percent"]
cache_strategy = "realtime"

[fields."space.block_storage.volume_deduplication_shared_count"]
cache_strategy = "realtime"

[fields."space.block_storage.volume_deduplication_space_saved"]
cache_strategy = "realtime"

[fields."space.block_storage.volume_deduplication_space_saved_percent"]
cache_strategy = "realtime"

[fields."space.block_storage.volume_footprints_percent"]
cache_strategy = "realtime"

[fields."space.cloud_storage.used"]
cache_strategy = "realtime"

[fields."space.efficiency.auto_adaptive_compression_savings"]
cache_strategy = "realtime"

[fields."space.efficiency.cross_volume_background_dedupe"]
cache_strategy = "cache"

[fields."space.efficiency.cross_volume_dedupe_savings"]
cache_strategy = "realtime"

[fields."space.efficiency.cross_volume_inline_dedupe"]
cache_strategy = "cache"

[fields."space.efficiency.enable_workload_informed_tsse"]
cache_strategy = "cache"

[fields."space.efficiency.logical_used"]
cache_strategy = "realtime"

[fields."space.efficiency.ratio"]
cache_strategy = "realtime"

[fields."space.efficiency.savings"]
cache_strategy = "realtime"

[fields."space.efficiency.wise_tsse_min_used_capacity_pct"]
cache_strategy = "cache"

[fields."space.efficiency_without_snapshots.logical_used"]
cache_strategy = "realtime"

[fields."space.efficiency_without_snapshots.ratio"]
cache_strategy = "realtime"

[fields."space.efficiency_without_snapshots.savings"]
cache_strategy = "realtime"

[fields."space.efficiency_without_snapshots_flexclones.logical_used"]
cache_strategy = "realtime"

[fields."space.efficiency_without_snapshots_flexclones.ratio"]
cache_strategy = "realtime"

[fields."space.efficiency_without_snapshots_flexclones.savings"]
cache_strategy = "realtime"

[fields."space.footprint"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."space.snapshot.available"]
cache_strategy = "realtime"

[fields."space.snapshot.reserve_percent"]
cache_strategy = "cache"

[fields."space.snapshot.total"]
cache_strategy = "realtime"

[fields."space.snapshot.used"]
cache_strategy = "realtime"

[fields."space.snapshot.used_percent"]
cache_strategy = "realtime"

[fields.state]
cache_strategy = "cache"

[fields."statistics.iops_raw.other"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."statistics.iops_raw.read"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."statistics.iops_raw.total"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."statistics.iops_raw.write"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."statistics.latency_raw.other"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."statistics.latency_raw.read"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."statistics.latency_raw.total"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."statistics.latency_raw.write"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."statistics.status"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."statistics.throughput_raw.other"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."statistics.throughput_raw.read"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."statistics.throughput_raw.total"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."statistics.throughput_raw.write"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."statistics.timestamp"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields.uuid]
cache_strategy = "cache"

[fields.volume_count]
cache_strategy = "cache"
