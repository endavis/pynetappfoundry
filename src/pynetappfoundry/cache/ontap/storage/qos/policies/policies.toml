[endpoint]
path = "/storage/qos/policies"
schema = "qos_policy"
class_name = "OntapQosPolicy"

[fields."adaptive.absolute_min_iops"]
cache_strategy = "cache"

[fields."adaptive.block_size"]
cache_strategy = "cache"

[fields."adaptive.expected_iops"]
cache_strategy = "cache"

[fields."adaptive.expected_iops_allocation"]
cache_strategy = "cache"

[fields."adaptive.peak_iops"]
cache_strategy = "cache"

[fields."adaptive.peak_iops_allocation"]
cache_strategy = "cache"

[fields."fixed.capacity_shared"]
cache_strategy = "cache"

[fields."fixed.max_throughput_iops"]
cache_strategy = "cache"

[fields."fixed.max_throughput_mbps"]
cache_strategy = "cache"

[fields."fixed.min_throughput_iops"]
cache_strategy = "cache"

[fields."fixed.min_throughput_mbps"]
cache_strategy = "cache"

[fields.name]
cache_strategy = "cache"

[fields.object_count]
cache_strategy = "cache"

[fields.pgid]
cache_strategy = "cache"

[fields.policy_class]
cache_strategy = "cache"

[fields.scope]
cache_strategy = "cache"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"

[fields.uuid]
cache_strategy = "cache"
