[endpoint]
path = "/storage/luns"
schema = "lun"
class_name = "OntapLun"

[fields.attributes]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields.auto_delete]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields.class_]
cache_strategy = "cache"

[fields."clone.source.name"]
cache_strategy = "cache"

[fields."clone.source.uuid"]
cache_strategy = "cache"

[fields.comment]
cache_strategy = "cache"

[fields."consistency_group.name"]
cache_strategy = "cache"

[fields."consistency_group.uuid"]
cache_strategy = "cache"

[fields."convert.namespace.name"]
cache_strategy = "cache"

[fields."convert.namespace.uuid"]
cache_strategy = "cache"

[fields."copy_.destinations"]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields."copy_.source.max_throughput"]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields."copy_.source.name"]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields."copy_.source.peer.name"]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields."copy_.source.peer.uuid"]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields."copy_.source.progress.elapsed"]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields."copy_.source.progress.failure.arguments"]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields."copy_.source.progress.failure.code"]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields."copy_.source.progress.failure.message"]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields."copy_.source.progress.percent_complete"]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields."copy_.source.progress.state"]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields."copy_.source.progress.volume_snapshot_blocked"]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields."copy_.source.uuid"]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields.create_time]
cache_strategy = "cache"

[fields.enabled]
cache_strategy = "cache"

[fields."location.logical_unit"]
cache_strategy = "cache"

[fields."location.node.name"]
cache_strategy = "cache"

[fields."location.node.uuid"]
cache_strategy = "cache"

[fields."location.qtree.id"]
cache_strategy = "cache"

[fields."location.qtree.name"]
cache_strategy = "cache"

[fields."location.volume.name"]
cache_strategy = "cache"

[fields."location.volume.uuid"]
cache_strategy = "cache"

[fields.lun_maps]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields."metric.duration"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.iops.other"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.iops.read"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.iops.total"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.iops.write"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.latency.other"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.latency.read"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.latency.total"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.latency.write"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.status"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.throughput.other"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.throughput.read"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.throughput.total"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.throughput.write"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.timestamp"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."movement.max_throughput"]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields."movement.paths.destination"]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields."movement.paths.source"]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields."movement.progress.elapsed"]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields."movement.progress.failure.arguments"]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields."movement.progress.failure.code"]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields."movement.progress.failure.message"]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields."movement.progress.percent_complete"]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields."movement.progress.state"]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields."movement.progress.volume_snapshot_blocked"]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields.name]
cache_strategy = "cache"

[fields.os_type]
cache_strategy = "cache"

[fields."provisioning_options.auto"]
cache_strategy = "cache"

[fields."provisioning_options.count"]
cache_strategy = "cache"

[fields."provisioning_options.qos_policy.name"]
cache_strategy = "cache"

[fields."provisioning_options.qos_policy.uuid"]
cache_strategy = "cache"

[fields."provisioning_options.snapshot_policy.name"]
cache_strategy = "cache"

[fields."provisioning_options.snapshot_policy.uuid"]
cache_strategy = "cache"

[fields."provisioning_options.storage_service.name"]
cache_strategy = "cache"

[fields."provisioning_options.tiering.control"]
cache_strategy = "cache"

[fields."provisioning_options.tiering.object_stores"]
cache_strategy = "cache"

[fields."provisioning_options.tiering.policy"]
cache_strategy = "cache"

[fields."provisioning_options.use_mirrored_aggregates"]
cache_strategy = "cache"

[fields."qos_policy.name"]
cache_strategy = "cache"

[fields."qos_policy.uuid"]
cache_strategy = "cache"

[fields.serial_number]
cache_strategy = "cache"

[fields."space.efficiency_ratio"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."space.guarantee.requested"]
cache_strategy = "realtime"

[fields."space.guarantee.reserved"]
cache_strategy = "realtime"

[fields."space.physical_used"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."space.physical_used_by_snapshots"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."space.scsi_thin_provisioning_support_enabled"]
cache_strategy = "cache"

[fields."space.size"]
cache_strategy = "realtime"

[fields."space.used"]
cache_strategy = "realtime"

[fields."statistics.iops_raw.other"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."statistics.iops_raw.read"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."statistics.iops_raw.total"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."statistics.iops_raw.write"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."statistics.latency_raw.other"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."statistics.latency_raw.read"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."statistics.latency_raw.total"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."statistics.latency_raw.write"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."statistics.status"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."statistics.throughput_raw.other"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."statistics.throughput_raw.read"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."statistics.throughput_raw.total"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."statistics.throughput_raw.write"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."statistics.timestamp"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."status.container_state"]
cache_strategy = "cache"

[fields."status.mapped"]
cache_strategy = "cache"

[fields."status.read_only"]
cache_strategy = "cache"

[fields."status.state"]
cache_strategy = "cache"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"

[fields.uuid]
cache_strategy = "cache"

[fields."vvol.bindings"]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields."vvol.is_bound"]
cache_strategy = "cache"
