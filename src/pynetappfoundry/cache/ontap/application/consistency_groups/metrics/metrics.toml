[endpoint]
path = "/application/consistency-groups/{consistency_group.uuid}/metrics"
schema = "consistency_group_metrics_response"
class_name = "OntapConsistencyGroupMetricsResponse"

[fields.available_space]
cache_strategy = "cache"

[fields.duration]
cache_strategy = "cache"

[fields."iops.other"]
cache_strategy = "cache"

[fields."iops.read"]
cache_strategy = "cache"

[fields."iops.total"]
cache_strategy = "cache"

[fields."iops.write"]
cache_strategy = "cache"

[fields."latency.other"]
cache_strategy = "cache"

[fields."latency.read"]
cache_strategy = "cache"

[fields."latency.total"]
cache_strategy = "cache"

[fields."latency.write"]
cache_strategy = "cache"

[fields.size]
cache_strategy = "cache"

[fields.status]
cache_strategy = "cache"

[fields."throughput.other"]
cache_strategy = "cache"

[fields."throughput.read"]
cache_strategy = "cache"

[fields."throughput.total"]
cache_strategy = "cache"

[fields."throughput.write"]
cache_strategy = "cache"

[fields.timestamp]
cache_strategy = "cache"

[fields.used_space]
cache_strategy = "cache"
