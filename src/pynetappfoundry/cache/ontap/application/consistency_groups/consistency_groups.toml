[endpoint]
path = "/application/consistency-groups"
schema = "consistency_group_response"
class_name = "OntapConsistencyGroupResponse"

[fields."application.component_type"]
cache_strategy = "cache"

[fields."application.type_"]
cache_strategy = "cache"

[fields."clone.guarantee.type_"]
cache_strategy = "cache"

[fields."clone.is_flexclone"]
cache_strategy = "cache"

[fields."clone.parent_consistency_group.name"]
cache_strategy = "cache"

[fields."clone.parent_consistency_group.uuid"]
cache_strategy = "cache"

[fields."clone.parent_snapshot.name"]
cache_strategy = "cache"

[fields."clone.parent_snapshot.uuid"]
cache_strategy = "cache"

[fields."clone.parent_svm.name"]
cache_strategy = "cache"

[fields."clone.parent_svm.uuid"]
cache_strategy = "cache"

[fields."clone.split_complete_percent"]
cache_strategy = "cache"

[fields."clone.split_estimate"]
cache_strategy = "cache"

[fields."clone.split_initiated"]
cache_strategy = "cache"

[fields."clone.volume.prefix"]
cache_strategy = "cache"

[fields."clone.volume.suffix"]
cache_strategy = "cache"

[fields.consistency_groups]
cache_strategy = "cache"

[fields.luns]
cache_strategy = "cache"

[fields."metric.available_space"]
cache_strategy = "realtime"

[fields."metric.duration"]
cache_strategy = "realtime"

[fields."metric.iops.other"]
cache_strategy = "realtime"

[fields."metric.iops.read"]
cache_strategy = "realtime"

[fields."metric.iops.total"]
cache_strategy = "realtime"

[fields."metric.iops.write"]
cache_strategy = "realtime"

[fields."metric.latency.other"]
cache_strategy = "realtime"

[fields."metric.latency.read"]
cache_strategy = "realtime"

[fields."metric.latency.total"]
cache_strategy = "realtime"

[fields."metric.latency.write"]
cache_strategy = "realtime"

[fields."metric.size"]
cache_strategy = "realtime"

[fields."metric.status"]
cache_strategy = "realtime"

[fields."metric.throughput.other"]
cache_strategy = "realtime"

[fields."metric.throughput.read"]
cache_strategy = "realtime"

[fields."metric.throughput.total"]
cache_strategy = "realtime"

[fields."metric.throughput.write"]
cache_strategy = "realtime"

[fields."metric.timestamp"]
cache_strategy = "realtime"

[fields."metric.used_space"]
cache_strategy = "realtime"

[fields.name]
cache_strategy = "cache"

[fields.namespaces]
cache_strategy = "cache"

[fields."parent_consistency_group.name"]
cache_strategy = "cache"

[fields."parent_consistency_group.uuid"]
cache_strategy = "cache"

[fields."provisioning_options.action"]
cache_strategy = "cache"

[fields."provisioning_options.name"]
cache_strategy = "cache"

[fields."provisioning_options.storage_service.name"]
cache_strategy = "cache"

[fields."qos.policy.name"]
cache_strategy = "cache"

[fields."qos.policy.uuid"]
cache_strategy = "cache"

[fields.replicated]
cache_strategy = "cache"

[fields.replication_relationships]
cache_strategy = "cache"

[fields.replication_source]
cache_strategy = "cache"

[fields."restore_to.snapshot.name"]
cache_strategy = "cache"

[fields."restore_to.snapshot.uuid"]
cache_strategy = "cache"

[fields."snapshot_policy.name"]
cache_strategy = "cache"

[fields."snapshot_policy.uuid"]
cache_strategy = "cache"

[fields."space.available"]
cache_strategy = "realtime"

[fields."space.size"]
cache_strategy = "realtime"

[fields."space.used"]
cache_strategy = "realtime"

[fields."statistics.available_space"]
cache_strategy = "realtime"

[fields."statistics.iops_raw.other"]
cache_strategy = "realtime"

[fields."statistics.iops_raw.read"]
cache_strategy = "realtime"

[fields."statistics.iops_raw.total"]
cache_strategy = "realtime"

[fields."statistics.iops_raw.write"]
cache_strategy = "realtime"

[fields."statistics.latency_raw.other"]
cache_strategy = "realtime"

[fields."statistics.latency_raw.read"]
cache_strategy = "realtime"

[fields."statistics.latency_raw.total"]
cache_strategy = "realtime"

[fields."statistics.latency_raw.write"]
cache_strategy = "realtime"

[fields."statistics.size"]
cache_strategy = "realtime"

[fields."statistics.status"]
cache_strategy = "realtime"

[fields."statistics.throughput_raw.other"]
cache_strategy = "realtime"

[fields."statistics.throughput_raw.read"]
cache_strategy = "realtime"

[fields."statistics.throughput_raw.total"]
cache_strategy = "realtime"

[fields."statistics.throughput_raw.write"]
cache_strategy = "realtime"

[fields."statistics.timestamp"]
cache_strategy = "realtime"

[fields."statistics.used_space"]
cache_strategy = "realtime"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"

[fields."tiering.control"]
cache_strategy = "cache"

[fields."tiering.object_stores"]
cache_strategy = "cache"

[fields."tiering.policy"]
cache_strategy = "cache"

[fields.uuid]
cache_strategy = "cache"

[fields.volumes]
cache_strategy = "cache"
