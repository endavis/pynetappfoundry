[endpoint]
path = "/protocols/san/initiators"
schema = "initiator"
class_name = "OntapInitiator"

[fields.comment]
cache_strategy = "cache"

[fields.name]
cache_strategy = "cache"

[fields.protocol]
cache_strategy = "cache"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"
