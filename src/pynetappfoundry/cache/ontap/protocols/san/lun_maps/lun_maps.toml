[endpoint]
path = "/protocols/san/lun-maps"
schema = "lun_map"
class_name = "OntapLunMap"

[fields."igroup.initiators"]
cache_strategy = "cache"

[fields."igroup.name"]
cache_strategy = "cache"

[fields."igroup.os_type"]
cache_strategy = "cache"

[fields."igroup.protocol"]
cache_strategy = "cache"

[fields."igroup.replicated"]
cache_strategy = "cache"

[fields."igroup.uuid"]
cache_strategy = "cache"

[fields.logical_unit_number]
cache_strategy = "cache"

[fields."lun.name"]
cache_strategy = "cache"

[fields."lun.node.name"]
cache_strategy = "cache"

[fields."lun.node.uuid"]
cache_strategy = "cache"

[fields."lun.smbc.replicated"]
cache_strategy = "cache"

[fields."lun.uuid"]
cache_strategy = "cache"

[fields.reporting_nodes]
cache_strategy = "cache"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"
