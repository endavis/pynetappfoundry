[endpoint]
path = "/protocols/san/lun-maps/{lun.uuid}/{igroup.uuid}/reporting-nodes"
schema = "lun_map_reporting_node"
class_name = "OntapLunMapReportingNode"

[fields."igroup.uuid"]
cache_strategy = "cache"

[fields."lun.uuid"]
cache_strategy = "cache"

[fields.name]
cache_strategy = "cache"

[fields.uuid]
cache_strategy = "cache"
