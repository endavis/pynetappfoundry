[endpoint]
path = "/protocols/san/iscsi/credentials"
schema = "iscsi_credentials"
class_name = "OntapIscsiCredentials"

[fields.authentication_type]
cache_strategy = "cache"

[fields."chap.inbound.password"]
cache_strategy = "cache"

[fields."chap.inbound.user"]
cache_strategy = "cache"

[fields."chap.outbound.password"]
cache_strategy = "cache"

[fields."chap.outbound.user"]
cache_strategy = "cache"

[fields.initiator]
cache_strategy = "cache"

[fields."initiator_address.masks"]
cache_strategy = "cache"

[fields."initiator_address.ranges"]
cache_strategy = "cache"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"
