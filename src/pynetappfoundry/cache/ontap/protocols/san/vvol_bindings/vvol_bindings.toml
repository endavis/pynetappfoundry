[endpoint]
path = "/protocols/san/vvol-bindings"
schema = "vvol_binding"
class_name = "OntapVvolBinding"

[fields.count]
cache_strategy = "cache"

[fields.id]
cache_strategy = "cache"

[fields.is_optimal]
cache_strategy = "cache"

[fields."protocol_endpoint.name"]
cache_strategy = "cache"

[fields."protocol_endpoint.uuid"]
cache_strategy = "cache"

[fields.secondary_id]
cache_strategy = "cache"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"

[fields."vvol.name"]
cache_strategy = "cache"

[fields."vvol.uuid"]
cache_strategy = "cache"
