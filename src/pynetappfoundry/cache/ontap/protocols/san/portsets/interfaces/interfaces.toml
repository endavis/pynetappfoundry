[endpoint]
path = "/protocols/san/portsets/{portset.uuid}/interfaces"
schema = "portset_interface"
class_name = "OntapPortsetInterface"

[fields."fc.name"]
cache_strategy = "cache"

[fields."fc.uuid"]
cache_strategy = "cache"

[fields."fc.wwpn"]
cache_strategy = "cache"

[fields."ip.ip.address"]
cache_strategy = "cache"

[fields."ip.name"]
cache_strategy = "cache"

[fields."ip.uuid"]
cache_strategy = "cache"

[fields."portset.uuid"]
cache_strategy = "cache"

[fields.records]
cache_strategy = "cache"

[fields.uuid]
cache_strategy = "cache"
