[endpoint]
path = "/protocols/san/igroups/{igroup.uuid}/initiators"
schema = "igroup_initiator"
class_name = "OntapIgroupInitiator"

[fields.comment]
cache_strategy = "cache"

[fields."connectivity_tracking.alerts"]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields."connectivity_tracking.connection_state"]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields."connectivity_tracking.connections"]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields."igroup.name"]
cache_strategy = "cache"

[fields."igroup.uuid"]
cache_strategy = "cache"

[fields.name]
cache_strategy = "cache"

[fields."proximity.local_svm"]
cache_strategy = "cache"

[fields."proximity.peer_svms"]
cache_strategy = "cache"

[fields.records]
cache_strategy = "cache"
