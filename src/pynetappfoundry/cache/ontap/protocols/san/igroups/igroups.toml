[endpoint]
path = "/protocols/san/igroups"
schema = "igroup"
class_name = "OntapIgroup"

[fields.comment]
cache_strategy = "cache"

[fields."connectivity_tracking.alerts"]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields."connectivity_tracking.connection_state"]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields."connectivity_tracking.required_nodes"]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields.delete_on_unmap]
cache_strategy = "cache"

[fields.igroups]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields.initiators]
cache_strategy = "cache"

[fields.lun_maps]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields.name]
cache_strategy = "cache"

[fields.os_type]
cache_strategy = "cache"

[fields.parent_igroups]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields."portset.name"]
cache_strategy = "cache"

[fields."portset.uuid"]
cache_strategy = "cache"

[fields.protocol]
cache_strategy = "cache"

[fields."replication.error.igroup.local_svm"]
cache_strategy = "cache"

[fields."replication.error.igroup.name"]
cache_strategy = "cache"

[fields."replication.error.igroup.uuid"]
cache_strategy = "cache"

[fields."replication.error.summary.arguments"]
cache_strategy = "cache"

[fields."replication.error.summary.code"]
cache_strategy = "cache"

[fields."replication.error.summary.message"]
cache_strategy = "cache"

[fields."replication.peer_svm.name"]
cache_strategy = "cache"

[fields."replication.peer_svm.uuid"]
cache_strategy = "cache"

[fields."replication.state"]
cache_strategy = "cache"

[fields.supports_igroups]
cache_strategy = "cache"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"

[fields."target.firmware_revision"]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields."target.product_id"]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields."target.vendor_id"]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields.uuid]
cache_strategy = "cache"
