[endpoint]
path = "/protocols/fpolicy/{svm.uuid}/policies"
schema = "fpolicy_policy"
class_name = "OntapFpolicyPolicy"

[fields.allow_privileged_access]
cache_strategy = "cache"

[fields.enabled]
cache_strategy = "cache"

[fields."engine.name"]
cache_strategy = "cache"

[fields.events]
cache_strategy = "cache"

[fields.mandatory]
cache_strategy = "cache"

[fields.name]
cache_strategy = "cache"

[fields.passthrough_read]
cache_strategy = "cache"

[fields.persistent_store]
cache_strategy = "cache"

[fields.priority]
cache_strategy = "cache"

[fields.privileged_user]
cache_strategy = "cache"

[fields."scope.check_extensions_on_directories"]
cache_strategy = "cache"

[fields."scope.exclude_export_policies"]
cache_strategy = "cache"

[fields."scope.exclude_extension"]
cache_strategy = "cache"

[fields."scope.exclude_shares"]
cache_strategy = "cache"

[fields."scope.exclude_volumes"]
cache_strategy = "cache"

[fields."scope.include_export_policies"]
cache_strategy = "cache"

[fields."scope.include_extension"]
cache_strategy = "cache"

[fields."scope.include_shares"]
cache_strategy = "cache"

[fields."scope.include_volumes"]
cache_strategy = "cache"

[fields."scope.object_monitoring_with_no_extension"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"
