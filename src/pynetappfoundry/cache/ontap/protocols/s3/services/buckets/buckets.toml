[endpoint]
path = "/protocols/s3/services/{svm.uuid}/buckets"
schema = "s3_bucket_svm"
class_name = "OntapS3BucketSvm"

[fields.aggregates]
cache_strategy = "cache"

[fields."audit_event_selector.access"]
cache_strategy = "cache"

[fields."audit_event_selector.permission"]
cache_strategy = "cache"

[fields.comment]
cache_strategy = "cache"

[fields.constituents_per_aggregate]
cache_strategy = "cache"

[fields."cors.rules"]
cache_strategy = "cache"

[fields."encryption.enabled"]
cache_strategy = "cache"

[fields."lifecycle_management.rules"]
cache_strategy = "cache"

[fields.logical_used_size]
cache_strategy = "cache"

[fields.name]
cache_strategy = "cache"

[fields.nas_path]
cache_strategy = "cache"

[fields."policy.statements"]
cache_strategy = "cache"

[fields."protection_status.destination.is_cloud"]
cache_strategy = "cache"

[fields."protection_status.destination.is_external_cloud"]
cache_strategy = "cache"

[fields."protection_status.destination.is_ontap"]
cache_strategy = "cache"

[fields."protection_status.is_protected"]
cache_strategy = "cache"

[fields."qos_policy.max_throughput_iops"]
cache_strategy = "cache"

[fields."qos_policy.max_throughput_mbps"]
cache_strategy = "cache"

[fields."qos_policy.min_throughput_iops"]
cache_strategy = "cache"

[fields."qos_policy.min_throughput_mbps"]
cache_strategy = "cache"

[fields."qos_policy.name"]
cache_strategy = "cache"

[fields."qos_policy.uuid"]
cache_strategy = "cache"

[fields."retention.default_period"]
cache_strategy = "cache"

[fields."retention.mode"]
cache_strategy = "cache"

[fields.role]
cache_strategy = "cache"

[fields.size]
cache_strategy = "cache"

[fields."snapshot_policy.name"]
cache_strategy = "cache"

[fields."snapshot_policy.uuid"]
cache_strategy = "cache"

[fields.storage_service_level]
cache_strategy = "cache"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"

[fields.type_]
cache_strategy = "cache"

[fields.uuid]
cache_strategy = "cache"

[fields.versioning_state]
cache_strategy = "cache"

[fields."volume.name"]
cache_strategy = "cache"

[fields."volume.uuid"]
cache_strategy = "cache"
