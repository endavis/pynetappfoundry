[endpoint]
path = "/protocols/locks"
schema = "client_lock"
class_name = "OntapClientLock"

[fields."byte_lock.exclusive"]
cache_strategy = "cache"

[fields."byte_lock.length"]
cache_strategy = "cache"

[fields."byte_lock.mandatory"]
cache_strategy = "cache"

[fields."byte_lock.offset"]
cache_strategy = "cache"

[fields."byte_lock.soft"]
cache_strategy = "cache"

[fields."byte_lock.super"]
cache_strategy = "cache"

[fields.client_address]
cache_strategy = "cache"

[fields.constituent]
cache_strategy = "cache"

[fields.delegation]
cache_strategy = "cache"

[fields."interface.ip.address"]
cache_strategy = "cache"

[fields."interface.name"]
cache_strategy = "cache"

[fields."interface.uuid"]
cache_strategy = "cache"

[fields."node.name"]
cache_strategy = "cache"

[fields."node.uuid"]
cache_strategy = "cache"

[fields.oplock_level]
cache_strategy = "cache"

[fields.owner_id]
cache_strategy = "cache"

[fields.path]
cache_strategy = "cache"

[fields.protocol]
cache_strategy = "cache"

[fields."share_lock.mode"]
cache_strategy = "cache"

[fields."share_lock.soft"]
cache_strategy = "cache"

[fields."smb.connect_state"]
cache_strategy = "cache"

[fields."smb.open_group_id"]
cache_strategy = "cache"

[fields."smb.open_type"]
cache_strategy = "cache"

[fields.state]
cache_strategy = "cache"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"

[fields.type_]
cache_strategy = "cache"

[fields.uuid]
cache_strategy = "cache"

[fields."volume.name"]
cache_strategy = "cache"

[fields."volume.uuid"]
cache_strategy = "cache"
