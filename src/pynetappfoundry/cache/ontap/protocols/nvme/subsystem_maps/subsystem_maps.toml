[endpoint]
path = "/protocols/nvme/subsystem-maps"
schema = "nvme_subsystem_map"
class_name = "OntapNvmeSubsystemMap"

[fields.anagrpid]
cache_strategy = "cache"
requires_explicit_fetch = true

[fields."namespace.name"]
cache_strategy = "cache"

[fields."namespace.node.name"]
cache_strategy = "cache"

[fields."namespace.node.uuid"]
cache_strategy = "cache"

[fields."namespace.uuid"]
cache_strategy = "cache"

[fields.nsid]
cache_strategy = "cache"

[fields."subsystem.name"]
cache_strategy = "cache"

[fields."subsystem.uuid"]
cache_strategy = "cache"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"
