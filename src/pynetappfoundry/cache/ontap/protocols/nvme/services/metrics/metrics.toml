[endpoint]
path = "/protocols/nvme/services/{svm.uuid}/metrics"
schema = "performance_nvme_metric_response"
class_name = "OntapPerformanceNvmeMetricResponse"

[fields.duration]
cache_strategy = "cache"

[fields."fc.duration"]
cache_strategy = "cache"

[fields."fc.iops.other"]
cache_strategy = "cache"

[fields."fc.iops.read"]
cache_strategy = "cache"

[fields."fc.iops.total"]
cache_strategy = "cache"

[fields."fc.iops.write"]
cache_strategy = "cache"

[fields."fc.latency.other"]
cache_strategy = "cache"

[fields."fc.latency.read"]
cache_strategy = "cache"

[fields."fc.latency.total"]
cache_strategy = "cache"

[fields."fc.latency.write"]
cache_strategy = "cache"

[fields."fc.status"]
cache_strategy = "cache"

[fields."fc.throughput.read"]
cache_strategy = "cache"

[fields."fc.throughput.total"]
cache_strategy = "cache"

[fields."fc.throughput.write"]
cache_strategy = "cache"

[fields."iops.other"]
cache_strategy = "cache"

[fields."iops.read"]
cache_strategy = "cache"

[fields."iops.total"]
cache_strategy = "cache"

[fields."iops.write"]
cache_strategy = "cache"

[fields."latency.other"]
cache_strategy = "cache"

[fields."latency.read"]
cache_strategy = "cache"

[fields."latency.total"]
cache_strategy = "cache"

[fields."latency.write"]
cache_strategy = "cache"

[fields.status]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"

[fields."tcp.duration"]
cache_strategy = "cache"

[fields."tcp.iops.other"]
cache_strategy = "cache"

[fields."tcp.iops.read"]
cache_strategy = "cache"

[fields."tcp.iops.total"]
cache_strategy = "cache"

[fields."tcp.iops.write"]
cache_strategy = "cache"

[fields."tcp.latency.other"]
cache_strategy = "cache"

[fields."tcp.latency.read"]
cache_strategy = "cache"

[fields."tcp.latency.total"]
cache_strategy = "cache"

[fields."tcp.latency.write"]
cache_strategy = "cache"

[fields."tcp.status"]
cache_strategy = "cache"

[fields."tcp.throughput.read"]
cache_strategy = "cache"

[fields."tcp.throughput.total"]
cache_strategy = "cache"

[fields."tcp.throughput.write"]
cache_strategy = "cache"

[fields."throughput.read"]
cache_strategy = "cache"

[fields."throughput.total"]
cache_strategy = "cache"

[fields."throughput.write"]
cache_strategy = "cache"

[fields.timestamp]
cache_strategy = "cache"
