[endpoint]
path = "/protocols/nvme/interfaces"
schema = "nvme_interface"
class_name = "OntapNvmeInterface"

[fields.enabled]
cache_strategy = "cache"

[fields."fc_interface.port.name"]
cache_strategy = "cache"

[fields."fc_interface.port.node.name"]
cache_strategy = "cache"

[fields."fc_interface.port.uuid"]
cache_strategy = "cache"

[fields."fc_interface.wwnn"]
cache_strategy = "cache"

[fields."fc_interface.wwpn"]
cache_strategy = "cache"

[fields.interface_type]
cache_strategy = "cache"

[fields."ip_interface.ip.address"]
cache_strategy = "cache"

[fields."ip_interface.location.port.name"]
cache_strategy = "cache"

[fields."ip_interface.location.port.node.name"]
cache_strategy = "cache"

[fields."ip_interface.location.port.uuid"]
cache_strategy = "cache"

[fields.name]
cache_strategy = "cache"

[fields."node.name"]
cache_strategy = "cache"

[fields."node.uuid"]
cache_strategy = "cache"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"

[fields.transport_address]
cache_strategy = "cache"

[fields.transport_protocols]
cache_strategy = "cache"

[fields.uuid]
cache_strategy = "cache"
