[endpoint]
path = "/protocols/nvme/subsystem-controllers"
schema = "nvme_subsystem_controller"
class_name = "OntapNvmeSubsystemController"

[fields."admin_queue.depth"]
cache_strategy = "cache"

[fields."dh_hmac_chap.group_size"]
cache_strategy = "cache"

[fields."dh_hmac_chap.hash_function"]
cache_strategy = "cache"

[fields."dh_hmac_chap.mode"]
cache_strategy = "cache"

[fields."digest.data"]
cache_strategy = "cache"

[fields."digest.header"]
cache_strategy = "cache"

[fields."host.id"]
cache_strategy = "cache"

[fields."host.nqn"]
cache_strategy = "cache"

[fields."host.transport_address"]
cache_strategy = "cache"

[fields.id]
cache_strategy = "cache"

[fields."interface.name"]
cache_strategy = "cache"

[fields."interface.transport_address"]
cache_strategy = "cache"

[fields."interface.uuid"]
cache_strategy = "cache"

[fields."io_queue.count"]
cache_strategy = "cache"

[fields."io_queue.depth"]
cache_strategy = "cache"

[fields.keep_alive_timeout]
cache_strategy = "cache"

[fields."node.name"]
cache_strategy = "cache"

[fields."node.uuid"]
cache_strategy = "cache"

[fields."subsystem.name"]
cache_strategy = "cache"

[fields."subsystem.uuid"]
cache_strategy = "cache"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"

[fields."tls.cipher"]
cache_strategy = "cache"

[fields."tls.key_type"]
cache_strategy = "cache"

[fields."tls.psk_identity"]
cache_strategy = "cache"

[fields.transport_protocol]
cache_strategy = "cache"
