[endpoint]
path = "/protocols/nvme/subsystems/{subsystem.uuid}/hosts"
schema = "nvme_subsystem_host"
class_name = "OntapNvmeSubsystemHost"

[fields."dh_hmac_chap.controller_secret_key"]
cache_strategy = "cache"

[fields."dh_hmac_chap.group_size"]
cache_strategy = "cache"

[fields."dh_hmac_chap.hash_function"]
cache_strategy = "cache"

[fields."dh_hmac_chap.host_secret_key"]
cache_strategy = "cache"

[fields."dh_hmac_chap.mode"]
cache_strategy = "cache"

[fields."io_queue.count"]
cache_strategy = "cache"

[fields."io_queue.depth"]
cache_strategy = "cache"

[fields.nqn]
cache_strategy = "cache"

[fields.priority]
cache_strategy = "cache"

[fields.records]
cache_strategy = "cache"

[fields."subsystem.name"]
cache_strategy = "cache"

[fields."subsystem.uuid"]
cache_strategy = "cache"

[fields."tls.configured_psk"]
cache_strategy = "cache"

[fields."tls.key_type"]
cache_strategy = "cache"
