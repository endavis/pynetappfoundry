[endpoint]
path = "/protocols/nvme/subsystems"
schema = "nvme_subsystem"
class_name = "OntapNvmeSubsystem"

[fields.comment]
cache_strategy = "cache"

[fields.delete_on_unmap]
cache_strategy = "cache"

[fields.hosts]
cache_strategy = "cache"

[fields."io_queue.default.count"]
cache_strategy = "cache"

[fields."io_queue.default.depth"]
cache_strategy = "cache"

[fields.name]
cache_strategy = "cache"

[fields.os_type]
cache_strategy = "cache"

[fields.serial_number]
cache_strategy = "cache"

[fields.subsystem_maps]
cache_strategy = "cache"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"

[fields.target_nqn]
cache_strategy = "cache"

[fields.uuid]
cache_strategy = "cache"

[fields.vendor_uuids]
cache_strategy = "cache"
