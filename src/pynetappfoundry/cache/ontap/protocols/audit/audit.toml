[endpoint]
path = "/protocols/audit"
schema = "audit"
class_name = "OntapAudit"

[fields.charge_qos]
cache_strategy = "cache"

[fields.enabled]
cache_strategy = "cache"

[fields."events.async_delete"]
cache_strategy = "cache"

[fields."events.audit_policy_change"]
cache_strategy = "cache"

[fields."events.authorization_policy"]
cache_strategy = "cache"

[fields."events.cap_staging"]
cache_strategy = "cache"

[fields."events.cifs_logon_logoff"]
cache_strategy = "cache"

[fields."events.file_operations"]
cache_strategy = "cache"

[fields."events.file_share"]
cache_strategy = "cache"

[fields."events.security_group"]
cache_strategy = "cache"

[fields."events.user_account"]
cache_strategy = "cache"

[fields.guarantee]
cache_strategy = "cache"

[fields."log.format"]
cache_strategy = "cache"

[fields."log.retention.count"]
cache_strategy = "cache"

[fields."log.retention.duration"]
cache_strategy = "cache"

[fields."log.rotation.now"]
cache_strategy = "cache"

[fields."log.rotation.schedule.days"]
cache_strategy = "cache"

[fields."log.rotation.schedule.hours"]
cache_strategy = "cache"

[fields."log.rotation.schedule.minutes"]
cache_strategy = "cache"

[fields."log.rotation.schedule.months"]
cache_strategy = "cache"

[fields."log.rotation.schedule.weekdays"]
cache_strategy = "cache"

[fields."log.rotation.size"]
cache_strategy = "cache"

[fields.log_path]
cache_strategy = "cache"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"
