[endpoint]
path = "/protocols/ndmp/sessions"
schema = "ndmp_session"
class_name = "OntapNdmpSession"

[fields.backup_engine]
cache_strategy = "cache"

[fields.client_address]
cache_strategy = "cache"

[fields.client_port]
cache_strategy = "cache"

[fields."data.bytes_processed"]
cache_strategy = "cache"

[fields."data.connection.address"]
cache_strategy = "cache"

[fields."data.connection.port"]
cache_strategy = "cache"

[fields."data.connection.type_"]
cache_strategy = "cache"

[fields."data.operation"]
cache_strategy = "cache"

[fields."data.reason"]
cache_strategy = "cache"

[fields."data.state"]
cache_strategy = "cache"

[fields.data_path]
cache_strategy = "cache"

[fields.id]
cache_strategy = "cache"

[fields."mover.bytes_moved"]
cache_strategy = "cache"

[fields."mover.connection.address"]
cache_strategy = "cache"

[fields."mover.connection.port"]
cache_strategy = "cache"

[fields."mover.connection.type_"]
cache_strategy = "cache"

[fields."mover.mode"]
cache_strategy = "cache"

[fields."mover.reason"]
cache_strategy = "cache"

[fields."mover.state"]
cache_strategy = "cache"

[fields."node.name"]
cache_strategy = "cache"

[fields."node.uuid"]
cache_strategy = "cache"

[fields."scsi.device_id"]
cache_strategy = "cache"

[fields."scsi.host_adapter"]
cache_strategy = "cache"

[fields."scsi.lun_id"]
cache_strategy = "cache"

[fields."scsi.target_id"]
cache_strategy = "cache"

[fields.source_address]
cache_strategy = "cache"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"

[fields.tape_device]
cache_strategy = "cache"

[fields.tape_mode]
cache_strategy = "cache"

[fields.uuid]
cache_strategy = "cache"
