[endpoint]
path = "/protocols/vscan/server-status"
schema = "vscan_server_status"
class_name = "OntapVscanServerStatus"

[fields.disconnected_reason]
cache_strategy = "cache"

[fields."interface.ip.address"]
cache_strategy = "cache"

[fields."interface.name"]
cache_strategy = "cache"

[fields."interface.uuid"]
cache_strategy = "cache"

[fields.ip]
cache_strategy = "cache"

[fields."node.name"]
cache_strategy = "cache"

[fields."node.uuid"]
cache_strategy = "cache"

[fields.state]
cache_strategy = "cache"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"

[fields.type_]
cache_strategy = "cache"

[fields.update_time]
cache_strategy = "cache"

[fields.vendor]
cache_strategy = "cache"

[fields.version]
cache_strategy = "cache"
