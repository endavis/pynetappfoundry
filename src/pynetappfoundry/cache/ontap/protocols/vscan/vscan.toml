[endpoint]
path = "/protocols/vscan/{svm.uuid}"
schema = "vscan"
class_name = "OntapVscan"

[fields.enabled]
cache_strategy = "cache"

[fields.mandatory]
cache_strategy = "cache"

[fields.name]
cache_strategy = "cache"

[fields."scope.exclude_extensions"]
cache_strategy = "cache"

[fields."scope.exclude_paths"]
cache_strategy = "cache"

[fields."scope.include_extensions"]
cache_strategy = "cache"

[fields."scope.max_file_size"]
cache_strategy = "cache"

[fields."scope.only_execute_access"]
cache_strategy = "cache"

[fields."scope.scan_readonly_volumes"]
cache_strategy = "cache"

[fields."scope.scan_without_extension"]
cache_strategy = "cache"
