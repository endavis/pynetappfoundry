[endpoint]
path = "/protocols/vscan/{svm.uuid}/scanner-pools"
schema = "vscan_scanner_pool"
class_name = "OntapVscanScannerPool"

[fields."cluster.name"]
cache_strategy = "cache"

[fields."cluster.uuid"]
cache_strategy = "cache"

[fields.name]
cache_strategy = "cache"

[fields.privileged_users]
cache_strategy = "cache"

[fields.role]
cache_strategy = "cache"

[fields.servers]
cache_strategy = "cache"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"
