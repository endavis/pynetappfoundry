[endpoint]
path = "/protocols/cifs/services"
schema = "cifs_service"
class_name = "OntapCifsService"

[fields."ad_domain.default_site"]
cache_strategy = "cache"

[fields."ad_domain.fqdn"]
cache_strategy = "cache"

[fields."ad_domain.organizational_unit"]
cache_strategy = "cache"

[fields."ad_domain.password"]
cache_strategy = "cache"

[fields."ad_domain.user"]
cache_strategy = "cache"

[fields.auth_style]
cache_strategy = "cache"

[fields.auth_user_type]
cache_strategy = "cache"

[fields.authentication_method]
cache_strategy = "cache"

[fields.client_certificate]
cache_strategy = "cache"

[fields.client_id]
cache_strategy = "cache"

[fields.client_secret]
cache_strategy = "cache"

[fields.comment]
cache_strategy = "cache"

[fields.default_unix_user]
cache_strategy = "cache"

[fields.enabled]
cache_strategy = "cache"

[fields.group_policy_object_enabled]
cache_strategy = "cache"

[fields.key_vault_uri]
cache_strategy = "cache"

[fields."metric.duration"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.iops.other"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.iops.read"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.iops.total"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.iops.write"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.latency.other"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.latency.read"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.latency.total"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.latency.write"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.status"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.throughput.read"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.throughput.total"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.throughput.write"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."metric.timestamp"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields.name]
cache_strategy = "cache"

[fields."netbios.aliases"]
cache_strategy = "cache"

[fields."netbios.enabled"]
cache_strategy = "cache"

[fields."netbios.wins_servers"]
cache_strategy = "cache"

[fields.oauth_host]
cache_strategy = "cache"

[fields."options.admin_to_root_mapping"]
cache_strategy = "cache"

[fields."options.advanced_sparse_file"]
cache_strategy = "cache"

[fields."options.backup_symlink_enabled"]
cache_strategy = "cache"

[fields."options.client_dup_detection_enabled"]
cache_strategy = "cache"

[fields."options.client_version_reporting_enabled"]
cache_strategy = "cache"

[fields."options.copy_offload"]
cache_strategy = "cache"

[fields."options.dac_enabled"]
cache_strategy = "cache"

[fields."options.export_policy_enabled"]
cache_strategy = "cache"

[fields."options.fake_open"]
cache_strategy = "cache"

[fields."options.fsctl_trim"]
cache_strategy = "cache"

[fields."options.junction_reparse"]
cache_strategy = "cache"

[fields."options.large_mtu"]
cache_strategy = "cache"

[fields."options.max_connections_per_session"]
cache_strategy = "cache"

[fields."options.max_lifs_per_session"]
cache_strategy = "cache"

[fields."options.max_opens_same_file_per_tree"]
cache_strategy = "cache"

[fields."options.max_same_tree_connect_per_session"]
cache_strategy = "cache"

[fields."options.max_same_user_sessions_per_connection"]
cache_strategy = "cache"

[fields."options.max_watches_set_per_tree"]
cache_strategy = "cache"

[fields."options.multichannel"]
cache_strategy = "cache"

[fields."options.null_user_windows_name"]
cache_strategy = "cache"

[fields."options.path_component_cache"]
cache_strategy = "cache"

[fields."options.referral"]
cache_strategy = "cache"

[fields."options.shadowcopy"]
cache_strategy = "cache"

[fields."options.shadowcopy_dir_depth"]
cache_strategy = "cache"

[fields."options.smb_credits"]
cache_strategy = "cache"

[fields."options.trusted_domain_enum_search_enabled"]
cache_strategy = "cache"

[fields."options.widelink_reparse_versions"]
cache_strategy = "cache"

[fields.proxy_host]
cache_strategy = "cache"

[fields.proxy_password]
cache_strategy = "cache"

[fields.proxy_port]
cache_strategy = "cache"

[fields.proxy_type]
cache_strategy = "cache"

[fields.proxy_username]
cache_strategy = "cache"

[fields."security.advertised_kdc_encryptions"]
cache_strategy = "cache"

[fields."security.aes_netlogon_enabled"]
cache_strategy = "cache"

[fields."security.encrypt_dc_connection"]
cache_strategy = "cache"

[fields."security.kdc_encryption"]
cache_strategy = "cache"

[fields."security.ldap_referral_enabled"]
cache_strategy = "cache"

[fields."security.lm_compatibility_level"]
cache_strategy = "cache"

[fields."security.restrict_anonymous"]
cache_strategy = "cache"

[fields."security.session_security"]
cache_strategy = "cache"

[fields."security.smb_encryption"]
cache_strategy = "cache"

[fields."security.smb_signing"]
cache_strategy = "cache"

[fields."security.try_ldap_channel_binding"]
cache_strategy = "cache"

[fields."security.use_ldaps"]
cache_strategy = "cache"

[fields."security.use_start_tls"]
cache_strategy = "cache"

[fields."statistics.iops_raw.other"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."statistics.iops_raw.read"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."statistics.iops_raw.total"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."statistics.iops_raw.write"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."statistics.latency_raw.other"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."statistics.latency_raw.read"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."statistics.latency_raw.total"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."statistics.latency_raw.write"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."statistics.status"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."statistics.throughput_raw.read"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."statistics.throughput_raw.total"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."statistics.throughput_raw.write"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."statistics.timestamp"]
cache_strategy = "realtime"
requires_explicit_fetch = true

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"

[fields.tenant_id]
cache_strategy = "cache"

[fields.timeout]
cache_strategy = "cache"

[fields.verify_host]
cache_strategy = "cache"

[fields.workgroup]
cache_strategy = "cache"
