[endpoint]
path = "/protocols/cifs/group-policies/{svm.uuid}/objects"
schema = "group_policy_object"
class_name = "OntapGroupPolicyObject"

[fields.central_access_policy_settings]
cache_strategy = "cache"

[fields.central_access_policy_staging_audit_type]
cache_strategy = "cache"

[fields.enabled]
cache_strategy = "cache"

[fields.extensions]
cache_strategy = "cache"

[fields.file_system_path]
cache_strategy = "cache"

[fields.index]
cache_strategy = "cache"

[fields.ldap_path]
cache_strategy = "cache"

[fields.link]
cache_strategy = "cache"

[fields.name]
cache_strategy = "cache"

[fields."registry_settings.branchcache.hash_publication_mode"]
cache_strategy = "cache"

[fields."registry_settings.branchcache.supported_hash_version"]
cache_strategy = "cache"

[fields."registry_settings.refresh_time_interval"]
cache_strategy = "cache"

[fields."registry_settings.refresh_time_random_offset"]
cache_strategy = "cache"

[fields."security_settings.event_audit_settings.logon_type"]
cache_strategy = "cache"

[fields."security_settings.event_audit_settings.object_access_type"]
cache_strategy = "cache"

[fields."security_settings.event_log_settings.max_size"]
cache_strategy = "cache"

[fields."security_settings.event_log_settings.retention_method"]
cache_strategy = "cache"

[fields."security_settings.files_or_folders"]
cache_strategy = "cache"

[fields."security_settings.kerberos.max_clock_skew"]
cache_strategy = "cache"

[fields."security_settings.kerberos.max_renew_age"]
cache_strategy = "cache"

[fields."security_settings.kerberos.max_ticket_age"]
cache_strategy = "cache"

[fields."security_settings.privilege_rights.change_notify_users"]
cache_strategy = "cache"

[fields."security_settings.privilege_rights.security_privilege_users"]
cache_strategy = "cache"

[fields."security_settings.privilege_rights.take_ownership_users"]
cache_strategy = "cache"

[fields."security_settings.registry_values.signing_required"]
cache_strategy = "cache"

[fields."security_settings.restrict_anonymous.anonymous_access_to_shares_and_named_pipes_restricted"]
cache_strategy = "cache"

[fields."security_settings.restrict_anonymous.combined_restriction_for_anonymous_user"]
cache_strategy = "cache"

[fields."security_settings.restrict_anonymous.no_enumeration_of_sam_accounts"]
cache_strategy = "cache"

[fields."security_settings.restrict_anonymous.no_enumeration_of_sam_accounts_and_shares"]
cache_strategy = "cache"

[fields."security_settings.restricted_groups"]
cache_strategy = "cache"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"

[fields.uuid]
cache_strategy = "cache"

[fields.version]
cache_strategy = "cache"
