[endpoint]
path = "/protocols/cifs/group-policies"
schema = "policies_and_rules_to_be_applied"
class_name = "OntapPoliciesAndRulesToBeApplied"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"

[fields."to_be_applied.access_policies"]
cache_strategy = "cache"

[fields."to_be_applied.access_rules"]
cache_strategy = "cache"

[fields."to_be_applied.objects"]
cache_strategy = "cache"

[fields."to_be_applied.restricted_groups"]
cache_strategy = "cache"
