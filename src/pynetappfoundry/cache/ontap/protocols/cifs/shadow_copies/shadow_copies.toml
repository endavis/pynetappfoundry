[endpoint]
path = "/protocols/cifs/shadow-copies"
schema = "shadowcopy"
class_name = "OntapShadowcopy"

[fields.client_uuid]
cache_strategy = "cache"

[fields.destination_dir]
cache_strategy = "cache"

[fields.files]
cache_strategy = "cache"

[fields."shadowcopy_set.uuid"]
cache_strategy = "cache"

[fields."share.name"]
cache_strategy = "cache"

[fields.source_dir]
cache_strategy = "cache"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"

[fields.uuid]
cache_strategy = "cache"

[fields."volume.name"]
cache_strategy = "cache"

[fields."volume.uuid"]
cache_strategy = "cache"

[fields.with_content]
cache_strategy = "cache"
