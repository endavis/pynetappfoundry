[endpoint]
path = "/protocols/cifs/domains"
schema = "cifs_domain"
class_name = "OntapCifsDomain"

[fields."ad_domain.password"]
cache_strategy = "cache"

[fields."ad_domain.user"]
cache_strategy = "cache"

[fields.client_certificate]
cache_strategy = "cache"

[fields.client_id]
cache_strategy = "cache"

[fields.discovered_servers]
cache_strategy = "cache"

[fields."name_mapping.trusted_domains"]
cache_strategy = "cache"

[fields."password_schedule.schedule_day_of_week"]
cache_strategy = "cache"

[fields."password_schedule.schedule_description"]
cache_strategy = "cache"

[fields."password_schedule.schedule_enabled"]
cache_strategy = "cache"

[fields."password_schedule.schedule_last_changed_time"]
cache_strategy = "cache"

[fields."password_schedule.schedule_randomized_minute"]
cache_strategy = "cache"

[fields."password_schedule.schedule_time_of_day"]
cache_strategy = "cache"

[fields."password_schedule.schedule_warn_message"]
cache_strategy = "cache"

[fields."password_schedule.schedule_weekly_interval"]
cache_strategy = "cache"

[fields.preferred_dcs]
cache_strategy = "cache"

[fields.server_discovery_mode]
cache_strategy = "cache"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"

[fields.tenant_id]
cache_strategy = "cache"

[fields.trust_relationships]
cache_strategy = "cache"
