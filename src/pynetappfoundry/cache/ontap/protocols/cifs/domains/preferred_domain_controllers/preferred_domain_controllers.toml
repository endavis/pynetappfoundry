[endpoint]
path = "/protocols/cifs/domains/{svm.uuid}/preferred-domain-controllers"
schema = "cifs_domain_preferred_dc"
class_name = "OntapCifsDomainPreferredDc"

[fields.fqdn]
cache_strategy = "cache"

[fields.server_ip]
cache_strategy = "cache"

[fields."status.details"]
cache_strategy = "cache"

[fields."status.reachable"]
cache_strategy = "cache"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"
