[endpoint]
path = "/protocols/cifs/unix-symlink-mapping"
schema = "cifs_symlink_mapping"
class_name = "OntapCifsSymlinkMapping"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"

[fields."target.home_directory"]
cache_strategy = "cache"

[fields."target.locality"]
cache_strategy = "cache"

[fields."target.path"]
cache_strategy = "cache"

[fields."target.server"]
cache_strategy = "cache"

[fields."target.share"]
cache_strategy = "cache"

[fields.unix_path]
cache_strategy = "cache"
