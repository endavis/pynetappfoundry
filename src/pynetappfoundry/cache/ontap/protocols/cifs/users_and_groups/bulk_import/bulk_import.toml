[endpoint]
path = "/protocols/cifs/users-and-groups/bulk-import/{svm.uuid}"
schema = "local_cifs_users_and_groups_import"
class_name = "OntapLocalCifsUsersAndGroupsImport"

[fields.decryption_password]
cache_strategy = "cache"

[fields."detailed_status.code"]
cache_strategy = "cache"

[fields."detailed_status.message"]
cache_strategy = "cache"

[fields.elements_ignored]
cache_strategy = "cache"

[fields.elements_imported]
cache_strategy = "cache"

[fields."import_uri.password"]
cache_strategy = "cache"

[fields."import_uri.path"]
cache_strategy = "cache"

[fields."import_uri.username"]
cache_strategy = "cache"

[fields.state]
cache_strategy = "cache"

[fields."status_uri.password"]
cache_strategy = "cache"

[fields."status_uri.path"]
cache_strategy = "cache"

[fields."status_uri.username"]
cache_strategy = "cache"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"
