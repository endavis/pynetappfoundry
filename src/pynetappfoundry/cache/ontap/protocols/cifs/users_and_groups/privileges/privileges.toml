[endpoint]
path = "/protocols/cifs/users-and-groups/privileges"
schema = "user_group_privileges"
class_name = "OntapUserGroupPrivileges"

[fields.name]
cache_strategy = "cache"

[fields.privileges]
cache_strategy = "cache"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"
