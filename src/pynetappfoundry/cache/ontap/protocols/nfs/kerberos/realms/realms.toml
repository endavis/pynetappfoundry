[endpoint]
path = "/protocols/nfs/kerberos/realms"
schema = "kerberos_realm"
class_name = "OntapKerberosRealm"

[fields."ad_server.address"]
cache_strategy = "cache"

[fields."ad_server.name"]
cache_strategy = "cache"

[fields."admin_server.address"]
cache_strategy = "cache"

[fields."admin_server.port"]
cache_strategy = "cache"

[fields.clock_skew]
cache_strategy = "cache"

[fields.comment]
cache_strategy = "cache"

[fields.encryption_types]
cache_strategy = "cache"

[fields."kdc.ip"]
cache_strategy = "cache"

[fields."kdc.port"]
cache_strategy = "cache"

[fields."kdc.vendor"]
cache_strategy = "cache"

[fields.name]
cache_strategy = "cache"

[fields."password_server.address"]
cache_strategy = "cache"

[fields."password_server.port"]
cache_strategy = "cache"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"
