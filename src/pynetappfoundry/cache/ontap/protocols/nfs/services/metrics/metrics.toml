[endpoint]
path = "/protocols/nfs/services/{svm.uuid}/metrics"
schema = "performance_svm_nfs_response"
class_name = "OntapPerformanceSvmNfsResponse"

[fields."v3.duration"]
cache_strategy = "cache"

[fields."v3.iops.other"]
cache_strategy = "cache"

[fields."v3.iops.read"]
cache_strategy = "cache"

[fields."v3.iops.total"]
cache_strategy = "cache"

[fields."v3.iops.write"]
cache_strategy = "cache"

[fields."v3.latency.other"]
cache_strategy = "cache"

[fields."v3.latency.read"]
cache_strategy = "cache"

[fields."v3.latency.total"]
cache_strategy = "cache"

[fields."v3.latency.write"]
cache_strategy = "cache"

[fields."v3.status"]
cache_strategy = "cache"

[fields."v3.throughput.read"]
cache_strategy = "cache"

[fields."v3.throughput.total"]
cache_strategy = "cache"

[fields."v3.throughput.write"]
cache_strategy = "cache"

[fields."v3.timestamp"]
cache_strategy = "cache"

[fields."v4.duration"]
cache_strategy = "cache"

[fields."v4.iops.other"]
cache_strategy = "cache"

[fields."v4.iops.read"]
cache_strategy = "cache"

[fields."v4.iops.total"]
cache_strategy = "cache"

[fields."v4.iops.write"]
cache_strategy = "cache"

[fields."v4.latency.other"]
cache_strategy = "cache"

[fields."v4.latency.read"]
cache_strategy = "cache"

[fields."v4.latency.total"]
cache_strategy = "cache"

[fields."v4.latency.write"]
cache_strategy = "cache"

[fields."v4.status"]
cache_strategy = "cache"

[fields."v4.throughput.read"]
cache_strategy = "cache"

[fields."v4.throughput.total"]
cache_strategy = "cache"

[fields."v4.throughput.write"]
cache_strategy = "cache"

[fields."v4.timestamp"]
cache_strategy = "cache"

[fields."v41.duration"]
cache_strategy = "cache"

[fields."v41.iops.other"]
cache_strategy = "cache"

[fields."v41.iops.read"]
cache_strategy = "cache"

[fields."v41.iops.total"]
cache_strategy = "cache"

[fields."v41.iops.write"]
cache_strategy = "cache"

[fields."v41.latency.other"]
cache_strategy = "cache"

[fields."v41.latency.read"]
cache_strategy = "cache"

[fields."v41.latency.total"]
cache_strategy = "cache"

[fields."v41.latency.write"]
cache_strategy = "cache"

[fields."v41.status"]
cache_strategy = "cache"

[fields."v41.throughput.read"]
cache_strategy = "cache"

[fields."v41.throughput.total"]
cache_strategy = "cache"

[fields."v41.throughput.write"]
cache_strategy = "cache"

[fields."v41.timestamp"]
cache_strategy = "cache"
