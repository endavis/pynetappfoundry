[endpoint]
path = "/name-services/cache/unix-user/settings"
schema = "unix_user_settings"
class_name = "OntapUnixUserSettings"

[fields.enabled]
cache_strategy = "cache"

[fields.negative_cache_enabled]
cache_strategy = "cache"

[fields.negative_ttl]
cache_strategy = "cache"

[fields.propagation_enabled]
cache_strategy = "cache"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"

[fields.ttl]
cache_strategy = "cache"
