[endpoint]
path = "/name-services/name-mappings"
schema = "name_mapping"
class_name = "OntapNameMapping"

[fields.client_match]
cache_strategy = "cache"

[fields.direction]
cache_strategy = "cache"

[fields.index]
cache_strategy = "cache"

[fields.pattern]
cache_strategy = "cache"

[fields.replacement]
cache_strategy = "cache"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"
