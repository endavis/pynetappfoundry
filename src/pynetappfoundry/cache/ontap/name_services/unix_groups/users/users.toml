[endpoint]
path = "/name-services/unix-groups/{svm.uuid}/{unix_group.name}/users"
schema = "unix_group_users"
class_name = "OntapUnixGroupUsers"

[fields.name]
cache_strategy = "cache"

[fields.records]
cache_strategy = "cache"

[fields.skip_name_validation]
cache_strategy = "cache"

[fields."svm.name"]
cache_strategy = "cache"

[fields."svm.uuid"]
cache_strategy = "cache"

[fields."unix_group.name"]
cache_strategy = "cache"
