[endpoint]
path = "/name-services/ldap-schemas"
schema = "ldap_schema"
class_name = "OntapLdapSchema"

[fields.comment]
cache_strategy = "cache"

[fields.global_schema]
cache_strategy = "cache"

[fields.name]
cache_strategy = "cache"

[fields."name_mapping.account.unix"]
cache_strategy = "cache"

[fields."name_mapping.account.windows"]
cache_strategy = "cache"

[fields."name_mapping.windows_to_unix.attribute"]
cache_strategy = "cache"

[fields."name_mapping.windows_to_unix.no_domain_prefix"]
cache_strategy = "cache"

[fields."name_mapping.windows_to_unix.object_class"]
cache_strategy = "cache"

[fields."owner.name"]
cache_strategy = "cache"

[fields."owner.uuid"]
cache_strategy = "cache"

[fields."rfc2307.attribute.gecos"]
cache_strategy = "cache"

[fields."rfc2307.attribute.gid_number"]
cache_strategy = "cache"

[fields."rfc2307.attribute.home_directory"]
cache_strategy = "cache"

[fields."rfc2307.attribute.login_shell"]
cache_strategy = "cache"

[fields."rfc2307.attribute.uid"]
cache_strategy = "cache"

[fields."rfc2307.attribute.uid_number"]
cache_strategy = "cache"

[fields."rfc2307.attribute.user_password"]
cache_strategy = "cache"

[fields."rfc2307.cn.group"]
cache_strategy = "cache"

[fields."rfc2307.cn.netgroup"]
cache_strategy = "cache"

[fields."rfc2307.member.nis_netgroup"]
cache_strategy = "cache"

[fields."rfc2307.member.uid"]
cache_strategy = "cache"

[fields."rfc2307.nis.mapentry"]
cache_strategy = "cache"

[fields."rfc2307.nis.mapname"]
cache_strategy = "cache"

[fields."rfc2307.nis.netgroup"]
cache_strategy = "cache"

[fields."rfc2307.nis.netgroup_triple"]
cache_strategy = "cache"

[fields."rfc2307.nis.object"]
cache_strategy = "cache"

[fields."rfc2307.posix.account"]
cache_strategy = "cache"

[fields."rfc2307.posix.group"]
cache_strategy = "cache"

[fields."rfc2307bis.enabled"]
cache_strategy = "cache"

[fields."rfc2307bis.group_of_unique_names"]
cache_strategy = "cache"

[fields."rfc2307bis.maximum_groups"]
cache_strategy = "cache"

[fields."rfc2307bis.unique_member"]
cache_strategy = "cache"

[fields.scope]
cache_strategy = "cache"

[fields."template.name"]
cache_strategy = "cache"
