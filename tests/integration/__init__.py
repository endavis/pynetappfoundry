"""Integration tests for pynetappfoundry.

Integration tests verify the interaction between components and external
systems. These tests require more setup than unit tests and may need
mocked external services or actual test environments.

To run integration tests:
    uv run pytest tests/integration/ -v

To skip integration tests during normal test runs:
    uv run pytest tests/unit/ -v
"""
